from std::collections import ArrayVec
from std::core import Clone, Option, Result
from amiga::workbench import command_line
from std::io import print_str
from std::memory import Slice
from std::string import FixedCString, FixedString, Str
from amiga::storage import BlockDevice, StorageDevice, devices, driver_is_loaded, load_driver
from amiga::ui import Bounds, Event, FileRequester, UiError, Window, WindowBuilder
from fsload import load_file_system
from filesystem_pool import FileSystemPool, file_system_preset, preset_dos_type
from format import format_partition
from rdb import DOS_TYPE_FFS_INTL, PartitionSettings, RdbModel, cylinders_to_mb, mb_to_cylinders
from rdb_io import load_rdb, write_rdb
from safety import DeviceIdentity, DeviceLiveness, classify_live_device, mounted_device_name_exists, mounted_partition_name

const DEVICE: u16 = 1
const PARTITIONS: u16 = 2
const NAME: u16 = 3
const SIZE: u16 = 4
const NEW: u16 = 5
const EDIT: u16 = 6
const DELETE: u16 = 7
const INIT: u16 = 8
const SAVE: u16 = 9
const REFRESH: u16 = 10
const SPLIT: u16 = 11
const RESIZE: u16 = 12
const FORMAT: u16 = 13
const FILE_SYSTEMS: u16 = 14
const STATUS: u16 = 15
const QUIT: u16 = 16
const FILE_SYSTEM: u16 = 17
const INTERNATIONAL: u16 = 18
const DIR_CACHE: u16 = 19
const BOOTABLE: u16 = 20
const BOOT_PRIORITY: u16 = 21
const MAX_TRANSFER: u16 = 22
const MASK: u16 = 23
const RESIZE_ANCHOR: u16 = 24
const LOAD_DRIVER: u16 = 26
const UNIT: u16 = 27
const SCAN: u16 = 28

const FS_LIST: u16 = 101
const FS_DOS_TYPE: u16 = 102
const FS_PRESET: u16 = 103
const FS_SOURCE_DRIVER: u16 = 104
const FS_ADD: u16 = 105
const FS_COPY: u16 = 106
const FS_REMOVE: u16 = 107
const FS_APPLY: u16 = 108
const FS_DONE: u16 = 109
const FS_SOURCE_UNIT: u16 = 110

const TEXT_DIALOG_INPUT: u16 = 301
const TEXT_DIALOG_OK: u16 = 302
const TEXT_DIALOG_CANCEL: u16 = 303

const EDIT_MAX_TRANSFER_PRESET: u16 = 201
const EDIT_MAX_TRANSFER_HELP: u16 = 202
const EDIT_MASK_PRESET: u16 = 203
const EDIT_MASK_HELP: u16 = 204
const EDIT_SIZE_HELP: u16 = 205
const EDIT_OK: u16 = 206
const EDIT_CANCEL: u16 = 207
const RESIZE_MAX_HELP: u16 = 208
const RESIZE_RANGE_HELP: u16 = 209
const RESIZE_OK: u16 = 210
const RESIZE_CANCEL: u16 = 211
const SPLIT_LESS: u16 = 212
const SPLIT_COUNT: u16 = 213
const SPLIT_MORE: u16 = 214
const SPLIT_PREVIEW: u16 = 215
const SPLIT_OK: u16 = 216
const SPLIT_CANCEL: u16 = 217

const MENU_PROJECT: u16 = 0
const MENU_DISK: u16 = 1
const MENU_PARTITION: u16 = 2

const PROJECT_ABOUT: u16 = 0
const PROJECT_SAVE: u16 = 2
const PROJECT_QUIT: u16 = 4
const DISK_SCAN: u16 = 0
const DISK_LOAD_DRIVER: u16 = 1
const DISK_REFRESH: u16 = 3
const DISK_INIT: u16 = 4
const DISK_FILE_SYSTEMS: u16 = 5
const PART_NEW: u16 = 0
const PART_EDIT: u16 = 1
const PART_DELETE: u16 = 2
const PART_SPLIT: u16 = 4
const PART_RESIZE: u16 = 5
const PART_FORMAT: u16 = 6

struct DiskEntry {
    driver: FixedCString<60>,
    unit: u32,
    block_bytes: u32,
    total_blocks: u32,
    cylinders: u32,
    heads: u32,
    sectors: u32,
}

struct ResizePlan {
    low: u32,
    high: u32,
}

impl DiskEntry {
    fn model(&self) -> RdbModel {
        var heads = self.heads
        var sectors = self.sectors
        if heads == 0 { heads = 16 }
        if sectors == 0 { sectors = 63 }
        var cylinders = self.cylinders
        if cylinders == 0 { cylinders = self.total_blocks / (heads * sectors) }
        return RdbModel::new(cylinders, heads, sectors)
    }
}

@suppress("W0002", "A decimal digit is provably in the ASCII 0-9 range")
fn append_u32(out: &var FixedString<80>, value: u32) -> bool {
    return out.push_u32(value).is_ok()
}

fn append(out: &var FixedString<80>, value: Str) -> bool {
    return out.push_str(value).is_ok()
}

@suppress("W0002", "GadTools indices come from fixed collections capped below 65536 entries")
fn gadget_index(value: usize) -> u16 { return (u16)value }

fn prefix(rows: Slice<Str>, count: usize) -> Option<Slice<Str>> {
    return rows.take(count)
}

fn probe_disk(driver: Str, unit: u32, disks: &var ArrayVec<DiskEntry, 32>) {
    for index in 0..disks.len() {
        let Option::Some(disk) = disks.get(index) else { continue }
        return if disk.unit == unit && disk.driver.as_str().eq_ignore_case(driver)
    }
    let Result::Ok(device) = BlockDevice::open(driver, unit) else { return }
    let geometry = device.geometry()
    let Result::Ok(owned) = FixedCString::<60>::new_from_str(driver) else { return }
    let _ = disks.push(DiskEntry {
        driver: owned,
        unit: unit,
        block_bytes: geometry.block_bytes(),
        total_blocks: geometry.total_blocks(),
        cylinders: geometry.cylinders(),
        heads: geometry.heads(),
        sectors: geometry.blocks_per_track(),
    })
}

fn known_drivers() -> ArrayVec<FixedCString<60>, 16> {
    let names = [
        "scsi.device", "2nd.scsi.device", "uaehf.device", "lide.device",
        "ide.device", "oktagon.device", "gvpscsi.device", "omniscsi.device",
        "a4091.device", "a3000scsi.device", "z3scsi.device",
    ]
    var drivers = ArrayVec::<FixedCString<60>, 16>::new()
    let _ = drivers.push(FixedCString::<60>::new())
    for index in 0..11 {
        continue unless driver_is_loaded(names[index])
        let Result::Ok(name) = FixedCString::<60>::new_from_str(names[index]) else { continue }
        let _ = drivers.push(name)
    }
    return drivers
}

fn add_driver(drivers: &var ArrayVec<FixedCString<60>, 16>, consuming name: FixedCString<60>) -> usize {
    for index in 0..drivers.len() {
        let Option::Some(current) = drivers.get(index) else { continue }
        return index if current.as_str().equals(name.as_str())
    }
    let index = drivers.len()
    let _ = drivers.push(name)
    return index
}

fn scan_driver(driver: Str) -> ArrayVec<DiskEntry, 32> {
    var disks = ArrayVec::<DiskEntry, 32>::new()
    for unit in 0..8 { probe_disk(driver, unit, &var disks) }
    return disks
}

fn next_available_partition_name(model: &RdbModel) -> Option<FixedString<8>> {
    for number in 0..1000 {
        var candidate = FixedString::<8>::new()
        continue if candidate.push_str("DH").is_err() || candidate.push_u32(number).is_err()
        continue if model.has_partition_named(candidate.as_str()) || mounted_device_name_exists(candidate.as_str())
        return Option::Some(candidate)
    }
    return Option::None
}

fn scan_all_drivers(
    drivers: &ArrayVec<FixedCString<60>, 16>,
    mounted: &ArrayVec<StorageDevice, 32>
) -> ArrayVec<DiskEntry, 32> {
    var disks = ArrayVec::<DiskEntry, 32>::new()
    for device in mounted { probe_disk(device.driver(), device.unit(), &var disks) }
    for driver in drivers {
        continue if driver.is_empty()
        for unit in 0..8 { probe_disk(driver.as_str(), unit, &var disks) }
    }
    return disks
}

fn scan_line(
    disk: &DiskEntry,
    mounted: &ArrayVec<StorageDevice, 32>
) -> FixedString<160> {
    var status = "blank"
    var label = ""
    for device in mounted {
        if device.unit() == disk.unit && device.driver().eq_ignore_case(disk.driver.as_str()) {
            status = "mounted"
            label = device.name()
        }
    }
    if status.equals("blank") {
        match BlockDevice::open(disk.driver.as_str(), disk.unit) {
            Result::Ok(var device) => {
                if load_rdb(&var device).is_ok() { status = "RDB" }
            },
            Result::Err(_) => { status = "no-media" },
        }
    }

    var line = FixedString::<160>::new()
    let _ = line.push_str(disk.driver.as_str())
    let _ = line.push_str(" unit ")
    let _ = line.push_u32(disk.unit)
    let _ = line.push_str("  ")
    let _ = line.push_u32(disk.total_blocks / 2048)
    let _ = line.push_str(" MB  [")
    let _ = line.push_str(status)
    let _ = line.push_byte((u8)']')
    if !label.is_empty() {
        let _ = line.push_str("  ")
        let _ = line.push_str(label)
    }
    let _ = line.push_byte((u8)'\n')
    return line
}

fn run_scan() -> i32 {
    return 20 if print_str("HDPart device scan\n------------------\n").is_err()
    let drivers = known_drivers()
    let mounted = match devices() {
        Result::Ok(value) => value,
        Result::Err(_) => ArrayVec::<StorageDevice, 32>::new(),
    }
    let disks = scan_all_drivers(&drivers, &mounted)
    if disks.is_empty() {
        return if print_str("(no block devices found)\n").is_ok() { 0 } else { 20 }
    }
    for disk in &disks {
        let line = scan_line(disk, &mounted)
        return 20 if print_str(line.as_str()).is_err()
    }
    return 0
}

fn set_status(window: &Window, value: Str) {
    let Option::Some(control) = window.text(STATUS) else { return }
    match control.set_text(value) {
        Result::Ok(_) => {},
        Result::Err(_) => {},
    }
}

fn refresh_driver_choices(window: &var Window, drivers: &ArrayVec<FixedCString<60>, 16>, active: u16) -> Result<(), UiError> {
    var views = [""; 16]
    for index in 0..drivers.len() {
        let Option::Some(driver) = drivers.get(index) else { continue }
        views[index] = if driver.is_empty() { "Select or load a driver" } else { driver.as_str() }
    }
    let Option::Some(used) = prefix(&views, drivers.len()) else { return Result::Err(UiError::ControlFailed) }
    window.replace_cycle(DEVICE, used, active)?
    return Result::Ok(())
}

fn refresh_unit_choices(window: &var Window, disks: &ArrayVec<DiskEntry, 32>) -> Result<(), UiError> {
    if disks.is_empty() {
        let empty = ["Press Scan"]
        window.replace_cycle(UNIT, &empty, 0)?
        set_control_enabled(window, UNIT, false)
        return Result::Ok(())
    }
    var storage = [FixedString::<80>::new(); 32]
    var views = [""; 32]
    for index in 0..disks.len() {
        let Option::Some(disk) = disks.get(index) else { continue }
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "unit ")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], disk.unit)
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "  ")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], disk.total_blocks / 2048)
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], " MB")
        views[index] = storage[index].as_str()
    }
    let Option::Some(used) = prefix(&views, disks.len()) else { return Result::Err(UiError::ControlFailed) }
    window.replace_cycle(UNIT, used, 0)?
    set_control_enabled(window, UNIT, true)
    return Result::Ok(())
}

fn refresh_partitions(window: &var Window, model: &RdbModel, selected: Option<usize>) -> Result<(), UiError> {
    if model.partition_count() == 0 {
        let empty = ["No partitions on this disk"]
        window.replace_list(PARTITIONS, &empty, Option::None)?
        draw_disk_map(window, model, Option::None)
        return Result::Ok(())
    }
    var storage = [FixedString::<80>::new(); 32]
    var views = [""; 32]
    for index in 0..model.partition_count() {
        let Option::Some(partition) = model.partition(index) else { continue }
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], partition.name())
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "  DOS\\")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], partition.dos_type() & $FF)
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "  cyl ")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], partition.low_cylinder())
        return Result::Err(UiError::ControlFailed) if storage[index].push_byte((u8)'-').is_err()
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], partition.high_cylinder())
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "  ")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], cylinders_to_mb(
            partition.high_cylinder() - partition.low_cylinder() + 1,
            model.cylinder_blocks(),
            model.block_bytes()
        ))
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], " MB")
        views[index] = storage[index].as_str()
    }
    let Option::Some(used) = prefix(&views, model.partition_count()) else { return Result::Err(UiError::ControlFailed) }
    window.replace_list(PARTITIONS, used, selected)?
    draw_disk_map(window, model, selected)
    return Result::Ok(())
}

fn clear_partitions(window: &var Window) -> Result<(), UiError> {
    let empty = ["No RDB on this disk"]
    window.replace_list(PARTITIONS, &empty, Option::None)?
    clear_disk_map(window)
    return Result::Ok(())
}

fn map_x(cylinder: u32, cylinders: u32, width: u32) -> u32 {
    return 0 if cylinders == 0
    return (cylinder / cylinders) * width + ((cylinder % cylinders) * width) / cylinders
}

@suppress("W0002", "The disk-map coordinates are bounded by the 630-pixel window")
fn clear_disk_map(window: &Window) {
    window.fill_rect(1, 18, 82, 611, 97)
    window.outline_rect(2, 18, 82, 611, 97)
}

@suppress("W0002", "The disk-map coordinates are bounded by the 630-pixel window")
fn draw_disk_map(window: &Window, model: &RdbModel, selected: Option<usize>) {
    clear_disk_map(window)
    return if model.cylinders() == 0
    for index in 0..model.partition_count() {
        let Option::Some(partition) = model.partition(index) else { continue }
        var left: u32 = 18 + map_x(partition.low_cylinder(), model.cylinders(), 594)
        var right: u32 = 18 + map_x(partition.high_cylinder() + 1, model.cylinders(), 594)
        if right <= left { right = left + 1 }
        if right > 612 { right = 612 }
        window.fill_rect(if (index & 1) == 0 { 3 } else { 2 }, (i16)left, 83, (i16)(right - 1), 96)
        match selected {
            Option::Some(value) => {
                if value == index {
                    window.outline_rect(1, (i16)left, 83, (i16)(right - 1), 96)
                }
            },
            Option::None => {},
        }
    }
}

@suppress("W0002", "Bounds checks prove the mouse coordinate and partition index fit their target types")
fn partition_at(model: &RdbModel, x: i16, y: i16) -> Option<usize> {
    return Option::None unless x >= 18 && x < 612 && y >= 82 && y < 98 && model.cylinders() > 0
    let position = (u32)(x - 18)
    for index in 0..model.partition_count() {
        let Option::Some(partition) = model.partition(index) else { continue }
        let left = map_x(partition.low_cylinder(), model.cylinders(), 594)
        var right = map_x(partition.high_cylinder() + 1, model.cylinders(), 594)
        if right <= left { right = left + 1 }
        return Option::Some(index) if position >= left && position < right
    }
    return Option::None
}

@suppress("W0002", "RDB supports at most 32 partitions")
fn partition_event_code(index: usize) -> u16 { return (u16)index }

fn select_partition(window: &Window, model: &RdbModel, index: usize) {
    let Option::Some(partition) = model.partition(index) else { return }
    match window.string(NAME) {
        Option::Some(control) => { let _ = control.set_value(partition.name()) },
        Option::None => {},
    }
    match window.integer(SIZE) {
        Option::Some(control) => control.set_u32_saturating(cylinders_to_mb(
            partition.high_cylinder() - partition.low_cylinder() + 1,
            model.cylinder_blocks(),
            model.block_bytes()
        )),
        Option::None => {},
    }
}

fn current_name(window: &Window) -> Option<Str> {
    let Option::Some(control) = window.string(NAME) else { return Option::None }
    return control.value()
}

fn current_size(window: &Window) -> Option<u32> {
    let Option::Some(control) = window.integer(SIZE) else { return Option::None }
    let Option::Some(value) = control.u32_value() else { return Option::None }
    return Option::None if value == 0
    return Option::Some(value)
}

fn hex_string(value: u32) -> FixedString<12> {
    let digits = "0123456789ABCDEF"
    var result = FixedString::<12>::new()
    let _ = result.push_str("0x")
    for shift in 0..8 {
        let nibble = (value >> ((7 - shift) * 4)) & 15
        let Option::Some(byte) = digits.get(nibble) else { continue }
        let _ = result.push_byte(byte)
    }
    return result
}

fn is_rom_dos_type(dos_type: u32) -> bool {
    return (dos_type & $FFFFFF00) == $444F5300 && (dos_type & $FF) <= 7
}

fn selected_dos_type(choice: u16, current: u32, window: &Window, pool: &FileSystemPool) -> u32 {
    if choice <= 1 {
        var value = if choice == 0 { $444F5301 } else { $444F5300 }
        let Option::Some(international) = window.checkbox(INTERNATIONAL) else { return value }
        let Option::Some(cache) = window.checkbox(DIR_CACHE) else { return value }
        if international.checked() { value = value | 2 }
        if cache.checked() { value = value | 4 }
        return value
    }
    let pool_index = (usize)choice - 2
    match pool.get(pool_index) {
        Option::Some(file_system) => return file_system.dos_type(),
        Option::None => {},
    }
    return current
}

fn refresh_partition_file_systems(window: &var Window, pool: &FileSystemPool, current: u32) -> Result<u16, UiError> {
    var storage = [FixedString::<12>::new(); 11]
    var views = [""; 11]
    let _ = storage[0].push_str("FFS")
    let _ = storage[1].push_str("OFS")
    views[0] = storage[0].as_str()
    views[1] = storage[1].as_str()
    var active: u16 = 0
    var matched = false
    for index in 0..pool.len() {
        let Option::Some(file_system) = pool.get(index) else { continue }
        let label = hex_string(file_system.dos_type())
        let _ = storage[index + 2].push_str(label.as_str())
        views[index + 2] = storage[index + 2].as_str()
        if file_system.dos_type() == current {
            active = gadget_index(index + 2)
            matched = true
        }
    }
    var count = pool.len() + 2
    if !matched {
        if is_rom_dos_type(current) {
            active = if (current & 1) != 0 { 0 } else { 1 }
        } else {
            let keep = count
            let label = hex_string(current)
            let _ = storage[keep].push_str(label.as_str())
            views[keep] = storage[keep].as_str()
            active = gadget_index(keep)
            count++
        }
    }
    let Option::Some(used) = prefix(&views, count) else { return Result::Err(UiError::ControlFailed) }
    window.replace_cycle(FILE_SYSTEM, used, active)?
    return Result::Ok(active)
}

fn set_partition_controls(window: &var Window, model: &RdbModel, index: usize, pool: &FileSystemPool) -> u16 {
    let Option::Some(partition) = model.partition(index) else { return 0 }
    match window.string(NAME) {
        Option::Some(control) => { let _ = control.set_value(partition.name()) },
        Option::None => {},
    }
    match window.integer(SIZE) {
        Option::Some(control) => control.set_u32_saturating(cylinders_to_mb(
            partition.high_cylinder() - partition.low_cylinder() + 1,
            model.cylinder_blocks(),
            model.block_bytes()
        )),
        Option::None => {},
    }
    match window.checkbox(BOOTABLE) {
        Option::Some(control) => control.set_checked(partition.bootable()),
        Option::None => {},
    }
    match window.integer(BOOT_PRIORITY) {
        Option::Some(control) => control.set_value(partition.boot_priority()),
        Option::None => {},
    }
    let max_transfer = hex_string(partition.max_transfer())
    let mask = hex_string(partition.mask())
    match window.string(MAX_TRANSFER) {
        Option::Some(control) => { let _ = control.set_value(max_transfer.as_str()) },
        Option::None => {},
    }
    match window.string(MASK) {
        Option::Some(control) => { let _ = control.set_value(mask.as_str()) },
        Option::None => {},
    }
    let choice = refresh_partition_file_systems(window, pool, partition.dos_type()).unwrap_or(0)
    match window.checkbox(INTERNATIONAL) {
        Option::Some(control) => control.set_checked((partition.dos_type() & 2) != 0),
        Option::None => {},
    }
    match window.checkbox(DIR_CACHE) {
        Option::Some(control) => control.set_checked((partition.dos_type() & 4) != 0),
        Option::None => {},
    }
    return choice
}

fn edit_partition(window: &Window, model: &var RdbModel, index: usize, file_system_choice: u16, pool: &FileSystemPool) -> bool {
    let Option::Some(previous) = model.partition(index) else { return false }
    let Option::Some(name) = current_name(window) else { return false }
    let Option::Some(size) = current_size(window) else { return false }
    let Option::Some(bootable) = window.checkbox(BOOTABLE) else { return false }
    let Option::Some(priority) = window.integer(BOOT_PRIORITY) else { return false }
    let Option::Some(max_transfer) = window.string(MAX_TRANSFER) else { return false }
    let Option::Some(mask) = window.string(MASK) else { return false }
    let Result::Ok(max_transfer_value) = max_transfer.value().unwrap_or("").parse_hex() else { return false }
    let Result::Ok(mask_value) = mask.value().unwrap_or("").parse_hex() else { return false }
    let Result::Ok(settings) = PartitionSettings::new(
        name,
        size,
        selected_dos_type(file_system_choice, previous.dos_type(), window, pool),
        bootable.checked(),
        priority.value().unwrap_or(0),
        max_transfer_value,
        mask_value
    ) else { return false }
    return model.update_partition_settings(index, settings).is_ok()
}

fn max_transfer_preset(value: u32) -> u16 {
    return 0 if value == $0001FE00
    return 1 if value == $00FFFFFF
    return 2 if value == $7FFFFFFF
    return 3
}

fn mask_preset(value: u32) -> u16 {
    return 0 if value == $7FFFFFFE
    return 1 if value == $00FFFFFF
    return 2 if value == $FFFFFFFC
    return 3 if value == $FFFFFFFF
    return 4
}

fn max_transfer_preset_value(index: u16) -> Option<u32> {
    return Option::Some($0001FE00) if index == 0
    return Option::Some($00FFFFFF) if index == 1
    return Option::Some($7FFFFFFF) if index == 2
    return Option::None
}

fn mask_preset_value(index: u16) -> Option<u32> {
    return Option::Some($7FFFFFFE) if index == 0
    return Option::Some($00FFFFFF) if index == 1
    return Option::Some($FFFFFFFC) if index == 2
    return Option::Some($FFFFFFFF) if index == 3
    return Option::None
}

@borrows(static)
fn max_transfer_guidance(index: u16) -> Str {
    return "0x1FE00: onboard IDE - required!" if index == 0
    return "0xFFFFFF: SCSI controllers" if index == 1
    return "0x7FFFFFFF: IDEfix/PFS3-AiO/PCMCIA" if index == 2
    return "Advanced - enter a hex value"
}

@borrows(static)
fn mask_guidance(index: u16) -> Str {
    return "DMA mask; OK as-is for plain IDE" if index == 0
    return "Zorro II DMA controller (24-bit)" if index == 1
    return "A1200/A4000 internal IDE" if index == 2
    return "No masking (Zorro III / no DMA)" if index == 3
    return "Advanced - enter a hex value"
}

fn apply_hex_preset(window: &Window, field: u16, help: u16, value: Option<u32>, guidance: Str) {
    set_control_enabled(window, field, value.is_none())
    match value {
        Option::Some(number) => {
            let text = hex_string(number)
            match window.string(field) {
                Option::Some(control) => { let _ = control.set_value(text.as_str()) },
                Option::None => {},
            }
        },
        Option::None => {},
    }
    match window.text(help) {
        Option::Some(control) => { let _ = control.set_text(guidance) },
        Option::None => {},
    }
}

@suppress("W0002", "GadTools integer fields and classic Amiga partition sizes are signed 32-bit")
fn partition_size_input(value: u32) -> i32 { return (i32)value }

fn partition_editor(
    parent: &Window,
    model: &var RdbModel,
    index: usize,
    pool: &FileSystemPool
) -> Result<bool, UiError> {
    let Option::Some(partition) = model.partition(index) else { return Result::Ok(false) }
    let size = cylinders_to_mb(
        partition.high_cylinder() - partition.low_cylinder() + 1,
        model.cylinder_blocks(),
        model.block_bytes()
    )
    let maximum_end = model.gap_end_after(index).unwrap_or(partition.high_cylinder())
    let maximum_size = cylinders_to_mb(
        maximum_end - partition.low_cylinder() + 1,
        model.cylinder_blocks(),
        model.block_bytes()
    )
    var size_help = FixedString::<32>::new()
    let _ = size_help.push_str("max ")
    let _ = size_help.push_u32(maximum_size)
    let _ = size_help.push_str(" MB")
    let max_transfer_text = hex_string(partition.max_transfer())
    let mask_text = hex_string(partition.mask())
    let max_transfer_choice = max_transfer_preset(partition.max_transfer())
    let mask_choice = mask_preset(partition.mask())

    var ui = parent.child_builder()?
    ui.add_string(NAME, "Name", Bounds::new(110, 18, 200, 14), partition.name(), 31)?
    ui.add_integer(SIZE, "Size (MB)", Bounds::new(110, 40, 90, 14), partition_size_input(size), 7)?
    ui.add_text(EDIT_SIZE_HELP, Bounds::new(220, 40, 130, 14), size_help.as_str())?
    let base_file_systems = ["FFS", "OFS"]
    ui.add_cycle(FILE_SYSTEM, "Filesystem", Bounds::new(110, 62, 130, 14), &base_file_systems, 0)?
    ui.add_checkbox(INTERNATIONAL, "International", Bounds::new(300, 64, 26, 11), (partition.dos_type() & 2) != 0)?
    ui.add_checkbox(DIR_CACHE, "Dir cache", Bounds::new(410, 64, 26, 11), (partition.dos_type() & 4) != 0)?
    ui.add_checkbox(BOOTABLE, "Bootable", Bounds::new(110, 88, 26, 11), partition.bootable())?
    ui.add_integer(BOOT_PRIORITY, "Boot priority", Bounds::new(270, 86, 70, 14), partition.boot_priority(), 6)?
    let max_transfer_choices = ["IDE", "SCSI", "No limit", "Custom"]
    ui.add_cycle(EDIT_MAX_TRANSFER_PRESET, "MaxTransfer", Bounds::new(110, 112, 130, 14), &max_transfer_choices, max_transfer_choice)?
    ui.add_string(MAX_TRANSFER, "", Bounds::new(270, 112, 120, 14), max_transfer_text.as_str(), 10)?
    ui.add_text(EDIT_MAX_TRANSFER_HELP, Bounds::new(18, 134, 470, 14), max_transfer_guidance(max_transfer_choice))?
    let mask_choices = ["Default", "Zorro II DMA", "A1200/A4000 IDE", "No mask", "Custom"]
    ui.add_cycle(EDIT_MASK_PRESET, "Mask", Bounds::new(110, 158, 160, 14), &mask_choices, mask_choice)?
    ui.add_string(MASK, "", Bounds::new(300, 158, 120, 14), mask_text.as_str(), 10)?
    ui.add_text(EDIT_MASK_HELP, Bounds::new(18, 180, 470, 14), mask_guidance(mask_choice))?
    ui.add_button(EDIT_OK, "OK", Bounds::new(18, 212, 90, 16))?
    ui.add_button(EDIT_CANCEL, "Cancel", Bounds::new(400, 212, 90, 16))?
    var menus = ui.menu_builder()
    var window_menu = menus.add_menu("GadToolsWindow")
    window_menu.add_item("Cancel").with_command_key((u8)'Q')
    let menu = menus.build()?
    var window = ui.build("Edit Partition", Bounds::new(60, 32, 510, 264), menu)?

    var file_system_choice = refresh_partition_file_systems(&var window, pool, partition.dos_type())?
    let rom_file_system = file_system_choice <= 1
    set_control_enabled(&window, INTERNATIONAL, rom_file_system)
    set_control_enabled(&window, DIR_CACHE, rom_file_system)
    apply_hex_preset(&window, MAX_TRANSFER, EDIT_MAX_TRANSFER_HELP, max_transfer_preset_value(max_transfer_choice), max_transfer_guidance(max_transfer_choice))
    apply_hex_preset(&window, MASK, EDIT_MASK_HELP, mask_preset_value(mask_choice), mask_guidance(mask_choice))

    forever {
        match window.wait_event() {
            Event::Close | Event::MenuPick(0, 0) | Event::GadgetUp(EDIT_CANCEL, _) => {
                return Result::Ok(false)
            },
            Event::Refresh => window.refresh(),
            Event::GadgetUp(FILE_SYSTEM, value) => {
                file_system_choice = value
                let rom = value <= 1
                set_control_enabled(&window, INTERNATIONAL, rom)
                set_control_enabled(&window, DIR_CACHE, rom)
            },
            Event::GadgetUp(EDIT_MAX_TRANSFER_PRESET, value) => {
                apply_hex_preset(&window, MAX_TRANSFER, EDIT_MAX_TRANSFER_HELP, max_transfer_preset_value(value), max_transfer_guidance(value))
            },
            Event::GadgetUp(EDIT_MASK_PRESET, value) => {
                apply_hex_preset(&window, MASK, EDIT_MASK_HELP, mask_preset_value(value), mask_guidance(value))
            },
            Event::GadgetUp(EDIT_OK, _) => {
                if edit_partition(&window, model, index, file_system_choice, pool) {
                    return Result::Ok(true)
                }
                window.alert("Edit Partition", "Check the name, size, MaxTransfer, Mask, and partition overlap.")
            },
            _ => {},
        }
    }
}

fn resize_plan(window: &Window, model: &RdbModel, index: usize, anchor: u16) -> Option<ResizePlan> {
    let Option::Some(partition) = model.partition(index) else { return Option::None }
    let Option::Some(requested_mb) = current_size(window) else { return Option::None }
    let maximum_cylinders = if anchor == 0 {
        model.gap_end_after(index).unwrap_or(partition.high_cylinder()) - partition.low_cylinder() + 1
    } else {
        partition.high_cylinder() - model.gap_start_before(index).unwrap_or(partition.low_cylinder()) + 1
    }
    var maximum_mb = cylinders_to_mb(maximum_cylinders, model.cylinder_blocks(), model.block_bytes())
    if maximum_mb == 0 { maximum_mb = 1 }
    var size_mb = requested_mb
    if size_mb > maximum_mb {
        size_mb = maximum_mb
        match window.integer(SIZE) {
            Option::Some(control) => control.set_u32_saturating(size_mb),
            Option::None => {},
        }
    }
    var cylinders = mb_to_cylinders(size_mb, model.cylinder_blocks(), model.block_bytes())
    if cylinders > maximum_cylinders { cylinders = maximum_cylinders }
    return if anchor == 0 {
        Option::Some(ResizePlan {
            low: partition.low_cylinder(),
            high: partition.low_cylinder() + cylinders - 1,
        })
    } else {
        Option::Some(ResizePlan {
            low: partition.high_cylinder() - cylinders + 1,
            high: partition.high_cylinder(),
        })
    }
}

fn refresh_resize_preview(window: &Window, model: &RdbModel, index: usize, anchor: u16) -> Option<ResizePlan> {
    let Option::Some(partition) = model.partition(index) else { return Option::None }
    let maximum_cylinders = if anchor == 0 {
        model.gap_end_after(index).unwrap_or(partition.high_cylinder()) - partition.low_cylinder() + 1
    } else {
        partition.high_cylinder() - model.gap_start_before(index).unwrap_or(partition.low_cylinder()) + 1
    }
    var maximum_mb = cylinders_to_mb(maximum_cylinders, model.cylinder_blocks(), model.block_bytes())
    if maximum_mb == 0 { maximum_mb = 1 }
    var maximum_text = FixedString::<32>::new()
    let _ = maximum_text.push_str("max ")
    let _ = maximum_text.push_u32(maximum_mb)
    let _ = maximum_text.push_str(" MB")
    match window.text(RESIZE_MAX_HELP) {
        Option::Some(control) => { let _ = control.set_text(maximum_text.as_str()) },
        Option::None => {},
    }
    let Option::Some(plan) = resize_plan(window, model, index, anchor) else { return Option::None }
    var range_text = FixedString::<64>::new()
    let _ = range_text.push_str("-> cylinders ")
    let _ = range_text.push_u32(plan.low)
    let _ = range_text.push_str("..")
    let _ = range_text.push_u32(plan.high)
    match window.text(RESIZE_RANGE_HELP) {
        Option::Some(control) => { let _ = control.set_text(range_text.as_str()) },
        Option::None => {},
    }
    return Option::Some(plan)
}

fn resize_partition_dialog(parent: &Window, model: &var RdbModel, index: usize) -> Result<bool, UiError> {
    let Option::Some(partition) = model.partition(index) else { return Result::Ok(false) }
    var size = cylinders_to_mb(
        partition.high_cylinder() - partition.low_cylinder() + 1,
        model.cylinder_blocks(),
        model.block_bytes()
    )
    if size == 0 { size = 1 }
    let anchors = ["Move: End edge", "Move: Start edge"]
    var ui = parent.child_builder()?
    ui.add_cycle(RESIZE_ANCHOR, "Anchor", Bounds::new(100, 18, 230, 14), &anchors, 0)?
    ui.add_integer(SIZE, "Size (MB)", Bounds::new(100, 40, 90, 14), partition_size_input(size), 7)?
    ui.add_text(RESIZE_MAX_HELP, Bounds::new(210, 40, 130, 14), "")?
    ui.add_text(RESIZE_RANGE_HELP, Bounds::new(18, 62, 330, 14), "")?
    ui.add_text(EDIT_SIZE_HELP, Bounds::new(18, 82, 360, 14), "NOTE: Resizing partitions implies data loss.")?
    ui.add_button(RESIZE_OK, "OK", Bounds::new(18, 108, 80, 16))?
    ui.add_button(RESIZE_CANCEL, "Cancel", Bounds::new(300, 108, 80, 16))?
    var menus = ui.menu_builder()
    var window_menu = menus.add_menu("GadToolsWindow")
    window_menu.add_item("Cancel").with_command_key((u8)'Q')
    let menu = menus.build()?
    let window = ui.build("Resize Partition", Bounds::new(100, 70, 400, 160), menu)?
    var anchor: u16 = 0
    let _ = refresh_resize_preview(&window, model, index, anchor)

    forever {
        match window.wait_event() {
            Event::Close | Event::MenuPick(0, 0) | Event::GadgetUp(RESIZE_CANCEL, _) => {
                return Result::Ok(false)
            },
            Event::Refresh => window.refresh(),
            Event::GadgetUp(RESIZE_ANCHOR, value) => {
                anchor = value
                let _ = refresh_resize_preview(&window, model, index, anchor)
            },
            Event::GadgetUp(SIZE, _) => {
                let _ = refresh_resize_preview(&window, model, index, anchor)
            },
            Event::GadgetUp(RESIZE_OK, _) => {
                let Option::Some(plan) = refresh_resize_preview(&window, model, index, anchor) else { continue }
                if plan.low == partition.low_cylinder() && plan.high == partition.high_cylinder() {
                    return Result::Ok(false)
                }
                let destructive = plan.low != partition.low_cylinder() || plan.high < partition.high_cylinder()
                if destructive && !window.confirm("Resize Partition", "Resizing changes the partition extent.\nReformat afterward; existing data will be lost.\nProceed?") {
                    continue
                }
                match model.resize_partition(index, plan.low, plan.high) {
                    Result::Ok(_) => return Result::Ok(true),
                    Result::Err(_) => window.alert("Resize Partition", "The requested size overlaps another partition or exceeds the disk."),
                }
            },
            _ => {},
        }
    }
}

fn refresh_split_preview(window: &Window, count: usize, span: u32, model: &RdbModel) {
    var count_text = FixedString::<8>::new()
    let _ = count_text.push_u32((u32)count)
    match window.text(SPLIT_COUNT) {
        Option::Some(control) => { let _ = control.set_text(count_text.as_str()) },
        Option::None => {},
    }
    var preview = FixedString::<48>::new()
    let _ = preview.push_u32((u32)count)
    let _ = preview.push_str(" x ~")
    let _ = preview.push_u32(cylinders_to_mb(span / (u32)count, model.cylinder_blocks(), model.block_bytes()))
    let _ = preview.push_str(" MB each")
    match window.text(SPLIT_PREVIEW) {
        Option::Some(control) => { let _ = control.set_text(preview.as_str()) },
        Option::None => {},
    }
}

fn split_dialog(parent: &Window, model: &RdbModel) -> Result<Option<usize>, UiError> {
    let Option::Some(gap) = model.largest_free_gap() else {
        parent.alert("Split", "There is no free space on this disk.")
        return Result::Ok(Option::None)
    }
    var maximum = (usize)gap.cylinders()
    let slots: usize = 32 - model.partition_count()
    if maximum > slots { maximum = slots }
    if maximum > 20 { maximum = 20 }
    if maximum == 0 {
        parent.alert("Split", "There is no room for another partition.")
        return Result::Ok(Option::None)
    }
    var count = if maximum < 4 { maximum } else { 4 }
    var ui = parent.child_builder()?
    ui.add_text(EDIT_SIZE_HELP, Bounds::new(18, 18, 82, 14), "Partitions")?
    ui.add_button(SPLIT_LESS, "<", Bounds::new(110, 16, 30, 16))?
    ui.add_text(SPLIT_COUNT, Bounds::new(148, 18, 44, 14), "4")?
    ui.add_button(SPLIT_MORE, ">", Bounds::new(200, 16, 30, 16))?
    ui.add_text(SPLIT_PREVIEW, Bounds::new(18, 44, 270, 14), "")?
    ui.add_button(SPLIT_OK, "OK", Bounds::new(18, 72, 80, 16))?
    ui.add_button(SPLIT_CANCEL, "Cancel", Bounds::new(210, 72, 80, 16))?
    var menus = ui.menu_builder()
    var window_menu = menus.add_menu("GadToolsWindow")
    window_menu.add_item("Cancel").with_command_key((u8)'Q')
    let menu = menus.build()?
    let window = ui.build("Split Remaining Free Space", Bounds::new(140, 90, 310, 124), menu)?
    refresh_split_preview(&window, count, gap.cylinders(), model)
    forever {
        match window.wait_event() {
            Event::Close | Event::MenuPick(0, 0) | Event::GadgetUp(SPLIT_CANCEL, _) => return Result::Ok(Option::None),
            Event::Refresh => window.refresh(),
            Event::GadgetUp(SPLIT_LESS, _) => {
                if count > 1 { count-- }
                refresh_split_preview(&window, count, gap.cylinders(), model)
            },
            Event::GadgetUp(SPLIT_MORE, _) => {
                if count < maximum { count++ }
                refresh_split_preview(&window, count, gap.cylinders(), model)
            },
            Event::GadgetUp(SPLIT_OK, _) => return Result::Ok(Option::Some(count)),
            _ => {},
        }
    }
}

fn set_control_enabled(window: &Window, id: u16, enabled: bool) {
    let Option::Some(control) = window.control(id) else { return }
    control.set_enabled(enabled)
}

fn refresh_file_system_rows(window: &var Window, pool: &FileSystemPool, selected: Option<usize>) -> Result<(), UiError> {
    if pool.is_empty() {
        let empty = ["No filesystem handlers in this session"]
        window.replace_list(FS_LIST, &empty, Option::None)?
        return Result::Ok(())
    }
    var storage = [FixedString::<80>::new(); 8]
    var views = [""; 8]
    for index in 0..pool.len() {
        let Option::Some(file_system) = pool.get(index) else { continue }
        let dos_type = hex_string(file_system.dos_type())
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], dos_type.as_str())
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "  v")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], file_system.version() >> 16)
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], ".")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], file_system.version() & $FFFF)
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "  ")
        return Result::Err(UiError::ControlFailed) unless append_u32(&var storage[index], (file_system.image().len() + 1023) / 1024)
        return Result::Err(UiError::ControlFailed) unless append(&var storage[index], "K")
        views[index] = storage[index].as_str()
    }
    let Option::Some(used) = prefix(&views, pool.len()) else { return Result::Err(UiError::ControlFailed) }
    window.replace_list(FS_LIST, used, selected)?
    return Result::Ok(())
}

fn set_file_system_editor(window: &Window, pool: &FileSystemPool, selected: Option<usize>) {
    let enabled = selected.is_some()
    set_control_enabled(window, FS_DOS_TYPE, enabled)
    set_control_enabled(window, FS_PRESET, enabled)
    set_control_enabled(window, FS_REMOVE, enabled)
    set_control_enabled(window, FS_APPLY, enabled)
    let Option::Some(index) = selected else { return }
    let Option::Some(file_system) = pool.get(index) else { return }
    let value = hex_string(file_system.dos_type())
    match window.string(FS_DOS_TYPE) {
        Option::Some(control) => { let _ = control.set_value(value.as_str()) },
        Option::None => {},
    }
    match window.cycle(FS_PRESET) {
        Option::Some(control) => control.set_active(file_system_preset(file_system.dos_type())),
        Option::None => {},
    }
}

fn text_dialog(
    parent: &Window,
    title: Str,
    label: Str,
    initial: Str,
    max_chars: u16
) -> Result<Option<FixedString<256>>, UiError> {
    var ui = parent.child_builder()?
    ui.add_string(TEXT_DIALOG_INPUT, label, Bounds::new(112, 18, 300, 14), initial, max_chars)?
    ui.add_button(TEXT_DIALOG_OK, "OK", Bounds::new(18, 48, 80, 16))?
    ui.add_button(TEXT_DIALOG_CANCEL, "Cancel", Bounds::new(332, 48, 80, 16))?
    var menus = ui.menu_builder()
    var window_menu = menus.add_menu("GadToolsWindow")
    window_menu.add_item("Cancel").with_command_key((u8)'Q')
    let menu = menus.build()?
    let window = ui.build(title, Bounds::new(80, 54, 430, 98), menu)?

    forever {
        match window.wait_event() {
            Event::Close | Event::MenuPick(0, 0) | Event::GadgetUp(TEXT_DIALOG_CANCEL, _) => {
                return Result::Ok(Option::None)
            },
            Event::Refresh => window.refresh(),
            Event::GadgetUp(TEXT_DIALOG_OK, _) | Event::GadgetUp(TEXT_DIALOG_INPUT, _) => {
                let Option::Some(control) = window.string(TEXT_DIALOG_INPUT) else { continue }
                let Option::Some(value) = control.value() else { continue }
                let Result::Ok(copy) = FixedString::<256>::new_from_str(value) else { continue }
                return Result::Ok(Option::Some(copy))
            },
            _ => {},
        }
    }
}

fn choose_file(
    parent: &Window,
    title: Str,
    initial_drawer: Str
) -> Result<Option<FixedString<256>>, UiError> {
    let requester = match FileRequester::new() {
        Result::Ok(value) => value,
        Result::Err(_) => return text_dialog(parent, title, "Full path", initial_drawer, 255),
    }
    let selected = match requester.choose_for(parent, title, initial_drawer, "", false) {
        Result::Ok(value) => value,
        Result::Err(_) => {
            parent.alert(title, "Could not read the selected path.")
            return Result::Ok(Option::None)
        },
    }
    return match selected {
        Option::Some(path) => match FixedString::<256>::new_from_str(path.as_str()) {
            Result::Ok(copy) => Result::Ok(Option::Some(copy)),
            Result::Err(_) => {
                parent.alert(title, "The selected path is too long.")
                Result::Ok(Option::None)
            },
        },
        Option::None => Result::Ok(Option::None),
    }
}

fn file_system_in_use(model: &RdbModel, dos_type: u32) -> bool {
    for index in 0..model.partition_count() {
        let Option::Some(partition) = model.partition(index) else { continue }
        return true if partition.dos_type() == dos_type
    }
    return false
}

fn manage_file_systems(parent: &Window, pool: &var FileSystemPool, drivers: &ArrayVec<FixedCString<60>, 16>, model: &RdbModel) -> Result<bool, UiError> {
    var ui = parent.child_builder()?
    let initial_rows = ["No filesystem handlers in this session"]
    ui.add_list_view(FS_LIST, Bounds::new(18, 18, 594, 88), &initial_rows, Option::None)?
    ui.add_string(FS_DOS_TYPE, "DosType", Bounds::new(86, 120, 110, 14), "0x00000000", 10)?
    let presets = ["DOS\\0", "DOS\\1", "DOS\\2", "DOS\\3", "DOS\\4", "DOS\\5", "PFS\\3", "PDS\\3", "SFS\\0", "SFS\\2", "Custom"]
    ui.add_cycle(FS_PRESET, "Preset", Bounds::new(276, 120, 126, 14), &presets, 10)?

    var driver_views = [""; 16]
    for index in 0..drivers.len() {
        let Option::Some(driver) = drivers.get(index) else { continue }
        driver_views[index] = if driver.is_empty() { "Select driver" } else { driver.as_str() }
    }
    let Option::Some(driver_choices) = prefix(&driver_views, drivers.len()) else { return Result::Err(UiError::ControlFailed) }
    let unit_choices = ["0", "1", "2", "3", "4", "5", "6", "7"]
    ui.add_cycle(FS_SOURCE_DRIVER, "Copy driver", Bounds::new(478, 120, 134, 14), driver_choices, 0)?
    ui.add_cycle(FS_SOURCE_UNIT, "Unit", Bounds::new(478, 142, 70, 14), &unit_choices, 0)?
    ui.add_button(FS_ADD, "Add file...", Bounds::new(18, 166, 100, 16))?
    ui.add_button(FS_COPY, "Copy disk", Bounds::new(124, 166, 92, 16))?
    ui.add_button(FS_REMOVE, "Remove", Bounds::new(222, 166, 82, 16))?
    ui.add_button(FS_APPLY, "Apply", Bounds::new(310, 166, 82, 16))?
    ui.add_button(FS_DONE, "Done", Bounds::new(530, 166, 82, 16))?
    var menus = ui.menu_builder()
    var window_menu = menus.add_menu("GadToolsWindow")
    window_menu.add_item("Done").with_command_key((u8)'Q')
    let menu = menus.build()?
    var window = ui.build("Filesystems", Bounds::new(0, 24, 630, 208), menu)?

    var selected: Option<usize> = if pool.is_empty() { Option::None } else { Option::Some(0) }
    var source_driver: usize = 0
    var source_unit: u32 = 0
    var changed = false
    refresh_file_system_rows(&var window, pool, selected)?
    set_file_system_editor(&window, pool, selected)
    set_control_enabled(&window, FS_COPY, drivers.len() > 1)

    forever {
        match window.wait_event() {
            Event::Close => { break },
            Event::MenuPick(0, 0) => { break },
            Event::Refresh => window.refresh(),
            Event::GadgetUp(FS_DONE, _) => { break },
            Event::GadgetUp(FS_SOURCE_DRIVER, value) => { source_driver = (usize)value },
            Event::GadgetUp(FS_SOURCE_UNIT, value) => { source_unit = (u32)value },
            Event::GadgetUp(FS_LIST, value) => {
                if (usize)value < pool.len() {
                    selected = Option::Some((usize)value)
                    refresh_file_system_rows(&var window, pool, selected)?
                    set_file_system_editor(&window, pool, selected)
                }
            },
            Event::GadgetUp(FS_PRESET, value) => {
                let Option::Some(index) = selected else { continue }
                let Option::Some(file_system) = pool.get(index) else { continue }
                let dos_type = preset_dos_type(value, file_system.dos_type())
                if value < 10 && pool.set_dos_type(index, dos_type).is_ok() {
                    changed = true
                    refresh_file_system_rows(&var window, pool, selected)?
                    set_file_system_editor(&window, pool, selected)
                }
            },
            Event::GadgetUp(FS_APPLY, _) | Event::GadgetUp(FS_DOS_TYPE, _) => {
                let Option::Some(index) = selected else { continue }
                let Option::Some(control) = window.string(FS_DOS_TYPE) else { continue }
                let Result::Ok(dos_type) = control.value().unwrap_or("").parse_hex() else {
                    window.alert("Filesystems", "Enter a hexadecimal DosType such as 0x50465303.")
                    continue
                }
                if pool.set_dos_type(index, dos_type).is_ok() {
                    changed = true
                    refresh_file_system_rows(&var window, pool, selected)?
                    set_file_system_editor(&window, pool, selected)
                }
            },
            Event::GadgetUp(FS_ADD, _) => {
                let selected_path = choose_file(&window, "Select a filesystem handler", "L:")?
                let Option::Some(path) = selected_path else { continue }
                let file_system = match load_file_system(path.as_str()) {
                    Result::Ok(value) => value,
                    Result::Err(error) => {
                        window.alert_error("Filesystems", error)
                        continue
                    },
                }
                match pool.add(file_system) {
                    Result::Ok(index) => {
                        selected = Option::Some(index)
                        changed = true
                        refresh_file_system_rows(&var window, pool, selected)?
                        set_file_system_editor(&window, pool, selected)
                    },
                    Result::Err(_) => window.alert("Filesystems", "The filesystem pool is full."),
                }
            },
            Event::GadgetUp(FS_COPY, _) => {
                let Option::Some(driver) = drivers.get(source_driver) else { continue }
                if driver.is_empty() {
                    window.alert("Filesystems", "Select a source driver and unit.")
                    continue
                }
                var device = match BlockDevice::open(driver.as_str(), source_unit) {
                    Result::Ok(value) => value,
                    Result::Err(_) => {
                        window.alert("Filesystems", "Could not open the source disk.")
                        continue
                    },
                }
                let source = match load_rdb(&var device) {
                    Result::Ok(value) => value,
                    Result::Err(_) => {
                        window.alert("Filesystems", "The source disk has no valid RDB.")
                        continue
                    },
                }
                let Option::Some(file_system) = source.file_system(0) else {
                    window.alert("Filesystems", "The source disk has no embedded filesystem.")
                    continue
                }
                let Option::Some(copy) = file_system.clone() else {
                    window.alert("Filesystems", "Not enough memory to copy the filesystem.")
                    continue
                }
                match pool.add(copy) {
                    Result::Ok(index) => {
                        selected = Option::Some(index)
                        changed = true
                        refresh_file_system_rows(&var window, pool, selected)?
                        set_file_system_editor(&window, pool, selected)
                    },
                    Result::Err(_) => window.alert("Filesystems", "The filesystem pool is full."),
                }
            },
            Event::GadgetUp(FS_REMOVE, _) => {
                let Option::Some(index) = selected else { continue }
                let Option::Some(file_system) = pool.get(index) else { continue }
                if file_system_in_use(model, file_system.dos_type()) && !window.confirm("Filesystems", "A partition uses this filesystem type. Remove it anyway?") {
                    continue
                }
                if pool.remove(index).is_ok() {
                    changed = true
                    if pool.is_empty() {
                        selected = Option::None
                    } else if index < pool.len() {
                        selected = Option::Some(index)
                    } else {
                        selected = Option::Some(pool.len() - 1)
                    }
                    refresh_file_system_rows(&var window, pool, selected)?
                    set_file_system_editor(&window, pool, selected)
                }
            },
            _ => {},
        }
    }
    return Result::Ok(changed)
}

fn save_model(window: &Window, disks: &ArrayVec<DiskEntry, 32>, disk_index: usize, model: &var RdbModel, pool: &FileSystemPool) -> bool {
    let Option::Some(disk) = disks.get(disk_index) else { return false }
    let identity = DeviceIdentity { driver: disk.driver.as_str(), unit: disk.unit }
    match classify_live_device(&identity) {
        DeviceLiveness::Boot => {
            return false unless window.confirm("Save", "WARNING: this is the boot device. Writing its RDB can corrupt the running system. Write anyway?")
        },
        DeviceLiveness::Mounted => {
            return false unless window.confirm("Save", "This disk has mounted volumes. Writing its RDB can disturb them. Proceed?")
        },
        DeviceLiveness::Clear => {},
    }
    return false unless window.confirm("Save", "Write and verify the partition table? This overwrites the disk RDB.")
    var device = match BlockDevice::open(disk.driver.as_str(), disk.unit) {
        Result::Ok(value) => value,
        Result::Err(_) => {
            window.alert("Save", "Could not open the block device.")
            return false
        },
    }
    if model.replace_used_file_systems(pool.entries()).is_err() {
        window.alert("Save", "Could not prepare the referenced filesystem handlers.")
        return false
    }
    match write_rdb(&var device, model) {
        Result::Ok(_) => {},
        Result::Err(_) => {
            window.alert("Save", "Writing the RDB failed.")
            return false
        },
    }
    let verified = match load_rdb(&var device) {
        Result::Ok(value) => value,
        Result::Err(_) => {
            window.alert("Save", "The RDB was written but read-back verification failed.")
            return false
        },
    }
    if verified.partition_count() != model.partition_count() {
        window.alert("Save", "The RDB read-back did not match the partition table.")
        return false
    }
    for index in 0..model.partition_count() {
        let Option::Some(expected) = model.partition(index) else { return false }
        let Option::Some(actual) = verified.partition(index) else { return false }
        if expected.low_cylinder() != actual.low_cylinder() || expected.high_cylinder() != actual.high_cylinder() || !expected.name().equals(actual.name()) {
            window.alert("Save", "The RDB read-back did not match the partition table.")
            return false
        }
    }
    window.alert("Save", "Saved and verified.")
    return true
}

fn update_action_state(window: &Window, has_driver: bool, has_disk: bool, has_model: bool, has_selection: bool, dirty: bool) {
    set_control_enabled(window, SCAN, has_driver)
    set_control_enabled(window, REFRESH, has_disk)
    set_control_enabled(window, INIT, has_disk)
    set_control_enabled(window, SAVE, has_model && dirty)
    set_control_enabled(window, NEW, has_model)
    set_control_enabled(window, SPLIT, has_disk)
    set_control_enabled(window, DELETE, has_selection)
    set_control_enabled(window, EDIT, has_selection)
    set_control_enabled(window, RESIZE, has_selection)
    set_control_enabled(window, FORMAT, has_selection)
    set_control_enabled(window, FILE_SYSTEMS, has_model)
    window.set_menu_enabled(MENU_PROJECT, PROJECT_SAVE, has_model && dirty)
    window.set_menu_enabled(MENU_DISK, DISK_SCAN, has_driver)
    window.set_menu_enabled(MENU_DISK, DISK_REFRESH, has_disk)
    window.set_menu_enabled(MENU_DISK, DISK_INIT, has_disk)
    window.set_menu_enabled(MENU_DISK, DISK_FILE_SYSTEMS, has_model)
    window.set_menu_enabled(MENU_PARTITION, PART_NEW, has_model)
    window.set_menu_enabled(MENU_PARTITION, PART_EDIT, has_selection)
    window.set_menu_enabled(MENU_PARTITION, PART_DELETE, has_selection)
    window.set_menu_enabled(MENU_PARTITION, PART_SPLIT, has_disk)
    window.set_menu_enabled(MENU_PARTITION, PART_RESIZE, has_selection)
    window.set_menu_enabled(MENU_PARTITION, PART_FORMAT, has_selection)
}

fn build_window() -> Result<Window, UiError> {
    var ui = WindowBuilder::workbench_or_own()?
    let no_drivers = ["Loading drivers..."]
    let no_units = ["Press Scan"]
    let no_partitions = ["Select a device"]
    ui.add_cycle(DEVICE, "Driver", Bounds::new(70, 18, 440, 14), &no_drivers, 0)?
    ui.add_button(LOAD_DRIVER, "Load...", Bounds::new(524, 18, 88, 14))?
    ui.add_cycle(UNIT, "Unit", Bounds::new(70, 40, 440, 14), &no_units, 0)?
    ui.add_button(SCAN, "Scan", Bounds::new(524, 40, 88, 14))?
    ui.add_button(FILE_SYSTEMS, "Filesystems...", Bounds::new(492, 62, 120, 14))?
    ui.add_list_view(PARTITIONS, Bounds::new(18, 112, 594, 74), &no_partitions, Option::None)?
    ui.add_button(NEW, "New", Bounds::new(18, 194, 62, 16))?
    ui.add_button(EDIT, "Edit...", Bounds::new(86, 194, 72, 16))?
    ui.add_button(DELETE, "Delete", Bounds::new(164, 194, 72, 16))?
    ui.add_button(SPLIT, "Split...", Bounds::new(242, 194, 74, 16))?
    ui.add_button(RESIZE, "Resize...", Bounds::new(322, 194, 88, 16))?
    ui.add_button(FORMAT, "Format...", Bounds::new(416, 194, 92, 16))?
    ui.add_text(STATUS, Bounds::new(18, 222, 390, 12), "Status: ready")?
    ui.add_button(REFRESH, "Refresh", Bounds::new(416, 220, 92, 16))?
    ui.add_button(SAVE, "Save", Bounds::new(514, 220, 98, 16))?

    var menus = ui.menu_builder()
    var project = menus.add_menu("Project")
    project.add_item("About HDPart...")
    project.add_separator()
    project.add_item("Save").with_command_key((u8)'S')
    project.add_separator()
    project.add_item("Quit").with_command_key((u8)'Q')
    var disk = menus.add_menu("Disk")
    disk.add_item("Scan").with_command_key((u8)'K')
    disk.add_item("Load Driver...").with_command_key((u8)'L')
    disk.add_separator()
    disk.add_item("Refresh").with_command_key((u8)'F')
    disk.add_item("Init Disk...").with_command_key((u8)'I')
    disk.add_item("Filesystems...").with_command_key((u8)'Y')
    var partition = menus.add_menu("Partition")
    partition.add_item("New").with_command_key((u8)'N')
    partition.add_item("Edit...").with_command_key((u8)'E')
    partition.add_item("Delete").with_command_key((u8)'D')
    partition.add_separator()
    partition.add_item("Split...").with_command_key((u8)'T')
    partition.add_item("Resize...").with_command_key((u8)'R')
    partition.add_item("Format...").with_command_key((u8)'O')
    let menu = menus.build()?
    return Result::Ok(ui.build("HDPart 0.9.1 — Novus", Bounds::new(0, 11, 630, 258), menu)?)
}

fn run() -> Result<(), UiError> {
    var window = build_window()?
    var drivers = known_drivers()
    var selected_driver: usize = 0
    var disks = ArrayVec::<DiskEntry, 32>::new()
    refresh_driver_choices(&var window, &drivers, 0)?
    refresh_unit_choices(&var window, &disks)?
    var selected_disk: usize = 0
    var selected_partition: Option<usize> = Option::None
    var model: Option<RdbModel> = Option::None
    var file_system_pool = FileSystemPool::new()
    var dirty = false

    clear_partitions(&var window)?
    set_status(&window, "Status: select a driver, then press Scan")

    forever {
        update_action_state(&window, selected_driver > 0, !disks.is_empty(), model.is_some(), selected_partition.is_some(), dirty)
        let event = window.wait_event()
        var action: u16 = 0
        var code: u16 = 0
        match event {
            Event::Close => { action = QUIT },
            Event::Refresh => {
                window.refresh()
                match &model {
                    Option::Some(&current) => draw_disk_map(&window, current, selected_partition),
                    Option::None => clear_disk_map(&window),
                }
            },
            Event::MenuPick(MENU_PROJECT, PROJECT_QUIT) => { action = QUIT },
            Event::MenuPick(MENU_PROJECT, PROJECT_ABOUT) => {
                window.alert("About HDPart", "HDPart 0.9.1\nA safe RDB partition editor written in Novus.")
            },
            Event::MenuPick(MENU_PROJECT, PROJECT_SAVE) => { action = SAVE },
            Event::MenuPick(MENU_DISK, DISK_SCAN) => { action = SCAN },
            Event::MenuPick(MENU_DISK, DISK_REFRESH) => { action = REFRESH },
            Event::MenuPick(MENU_DISK, DISK_INIT) => { action = INIT },
            Event::MenuPick(MENU_DISK, DISK_FILE_SYSTEMS) => { action = FILE_SYSTEMS },
            Event::MenuPick(MENU_DISK, DISK_LOAD_DRIVER) => { action = LOAD_DRIVER },
            Event::MenuPick(MENU_PARTITION, PART_NEW) => { action = NEW },
            Event::MenuPick(MENU_PARTITION, PART_EDIT) => { action = EDIT },
            Event::MenuPick(MENU_PARTITION, PART_DELETE) => { action = DELETE },
            Event::MenuPick(MENU_PARTITION, PART_SPLIT) => { action = SPLIT },
            Event::MenuPick(MENU_PARTITION, PART_RESIZE) => { action = RESIZE },
            Event::MenuPick(MENU_PARTITION, PART_FORMAT) => { action = FORMAT },
            Event::MouseButton(x, y, true) => {
                let Option::Some(&current) = &model else { continue }
                match partition_at(current, x, y) {
                    Option::Some(index) => {
                        action = PARTITIONS
                        code = partition_event_code(index)
                    },
                    Option::None => {},
                }
            },
            Event::GadgetUp(id, value) => {
                action = id
                code = value
            },
            _ => {},
        }

        if action == QUIT {
            if !dirty { break }
            match window.choice("Unsaved Changes", "Save the partition table before closing?", "Save|Discard|Cancel") {
                1 => {
                    let Option::Some(&var current) = &var model else { continue }
                    continue unless save_model(&window, &disks, selected_disk, current, &file_system_pool)
                    break
                },
                2 => { break },
                _ => { continue },
            }
        }

        let discards_model = action == SCAN || action == LOAD_DRIVER ||
            (action == DEVICE && (usize)code != selected_driver) ||
            (action == UNIT && (usize)code != selected_disk)
        if dirty && discards_model && !window.confirm("Unsaved Changes", "Discard unsaved changes and switch disks?") {
            match window.cycle(if action == DEVICE { DEVICE } else { UNIT }) {
                Option::Some(control) => control.set_active(gadget_index(if action == DEVICE { selected_driver } else { selected_disk })),
                Option::None => {},
            }
            continue
        }

        if action == DEVICE {
            selected_driver = (usize)code
            disks = ArrayVec::<DiskEntry, 32>::new()
            refresh_unit_choices(&var window, &disks)?
            selected_disk = 0
            selected_partition = Option::None
            model = Option::None
            dirty = false
            clear_partitions(&var window)?
            set_status(&window, if selected_driver == 0 { "Status: select a driver, then press Scan" } else { "Status: press Scan to query this driver" })
        }

        if action == REFRESH {
            if dirty && !window.confirm("Refresh", "Discard unsaved changes and reread this disk?") { continue }
            action = UNIT
            code = gadget_index(selected_disk)
        }

        if action == SCAN {
            let Option::Some(driver) = drivers.get(selected_driver) else { continue }
            continue if driver.is_empty()
            disks = scan_driver(driver.as_str())
            selected_disk = 0
            selected_partition = Option::None
            model = Option::None
            dirty = false
            refresh_unit_choices(&var window, &disks)?
            if disks.is_empty() {
                clear_partitions(&var window)?
                set_status(&window, "Status: no units responded on this driver")
            } else {
                action = UNIT
                code = 0
            }
        }

        if action == UNIT {
            selected_disk = (usize)code
            selected_partition = Option::None
            dirty = false
            let Option::Some(disk) = disks.get(selected_disk) else { continue }
            var device = match BlockDevice::open(disk.driver.as_str(), disk.unit) {
                Result::Ok(value) => value,
                Result::Err(_) => {
                    clear_partitions(&var window)?
                    set_status(&window, "Status: could not open device")
                    model = Option::None
                    continue
                },
            }
            match load_rdb(&var device) {
                Result::Ok(value) => {
                    let _ = file_system_pool.merge(&value)
                    refresh_partitions(&var window, &value, Option::None)?
                    set_status(&window, "Status: RDB loaded")
                    model = Option::Some(value)
                },
                Result::Err(_) => {
                    let blank_model = disk.model()
                    refresh_partitions(&var window, &blank_model, Option::None)?
                    set_status(&window, "Status: blank disk; changes are not written until Save")
                    model = Option::Some(blank_model)
                },
            }
        }

        if action == PARTITIONS {
            selected_partition = Option::Some((u32)code)
            let Option::Some(&current) = &model else { continue }
            refresh_partitions(&var window, current, selected_partition)?
        }

        if action == INIT {
            let Option::Some(disk) = disks.get(selected_disk) else { continue }
            continue unless window.confirm("Init Disk", "Replace the in-memory partition table with an empty one? Nothing is written until Save.")
            model = Option::Some(disk.model())
            selected_partition = Option::None
            let Option::Some(&current) = &model else { continue }
            refresh_partitions(&var window, current, Option::None)?
            set_status(&window, "Status: new empty RDB")
            dirty = true
        }

        if action == NEW {
            let Option::Some(&var current) = &var model else { continue }
            match current.largest_free_gap() {
                Option::Some(gap) => {
                    let Option::Some(name) = next_available_partition_name(current) else {
                        window.alert("New Partition", "No unused DOS device name is available.")
                        continue
                    }
            let previous_selection: Option<usize> = match &selected_partition {
                        Option::Some(&value) => Option::Some(*value),
                        Option::None => Option::None,
                    }
                    let previous_dirty = dirty
                    match current.add_partition_cylinders(name.as_str(), gap.start(), gap.end(), DOS_TYPE_FFS_INTL) {
                    Result::Ok(index) => {
                        selected_partition = Option::Some(index)
                        refresh_partitions(&var window, current, selected_partition)?
                        if partition_editor(&window, current, index, &file_system_pool)? {
                            refresh_partitions(&var window, current, selected_partition)?
                            set_status(&window, "Status: partition added; Save to apply")
                            dirty = true
                        } else {
                            let _ = current.delete_partition(index)
                            selected_partition = previous_selection
                            dirty = previous_dirty
                            refresh_partitions(&var window, current, selected_partition)?
                        }
                    },
                    Result::Err(_) => window.alert("New Partition", "The partition does not fit or its name is invalid."),
                    }
                },
                Option::None => window.alert("New Partition", "There is no free space on this disk."),
            }
        }

        if action == EDIT {
            let Option::Some(index) = selected_partition else { continue }
            let Option::Some(&var current) = &var model else { continue }
            if partition_editor(&window, current, index, &file_system_pool)? {
                refresh_partitions(&var window, current, selected_partition)?
                set_status(&window, "Status: partition updated; Save to apply")
                dirty = true
            }
        }

        if action == DELETE {
            let Option::Some(index) = selected_partition else { continue }
            continue unless window.confirm("Delete Partition", "Remove the selected partition from the in-memory table? Save applies the change.")
            let Option::Some(&var current) = &var model else { continue }
            match current.delete_partition((u32)index) {
                Result::Ok(_) => {
                    selected_partition = Option::None
                    refresh_partitions(&var window, current, Option::None)?
                    set_status(&window, "Status: partition removed; Save to apply")
                    dirty = true
                },
                Result::Err(_) => {},
            }
        }

        if action == RESIZE {
            let Option::Some(index) = selected_partition else { continue }
            let Option::Some(&var current) = &var model else { continue }
            if resize_partition_dialog(&window, current, index)? {
                refresh_partitions(&var window, current, selected_partition)?
                set_status(&window, "Status: partition resized; Save to apply")
                dirty = true
            }
        }

        if action == SPLIT {
            let Option::Some(&var current) = &var model else { continue }
            let Option::Some(count) = split_dialog(&window, current)? else { continue }
            match current.split_largest_gap(count, DOS_TYPE_FFS_INTL) {
                Result::Ok(_) => {
                    selected_partition = Option::None
                    refresh_partitions(&var window, current, Option::None)?
                    set_status(&window, "Status: free space split; Save to apply")
                    dirty = true
                },
                Result::Err(_) => window.alert("Split", "The free area cannot fit that many partitions."),
            }
        }
        if action == FORMAT {
            if dirty {
                window.alert("Format", "Save the partition table first, then format.")
                continue
            }
            let Option::Some(index) = selected_partition else { continue }
            let Option::Some(&current) = &model else { continue }
            let Option::Some(partition) = current.partition(index) else { continue }
            let Option::Some(disk) = disks.get(selected_disk) else { continue }
            let identity = DeviceIdentity { driver: disk.driver.as_str(), unit: disk.unit }
            if matches!(classify_live_device(&identity), DeviceLiveness::Boot) {
                window.alert("Format", "This is the boot device. Boot from another disk before formatting it.")
                continue
            }
            var warning = FixedString::<256>::new()
            let _ = warning.push_str("Format ")
            let _ = warning.push_str(partition.name())
            let _ = warning.push_str(" into an empty volume?")
            let mounted_partition = match mounted_partition_name(&identity, partition.low_cylinder(), partition.high_cylinder()) {
                Result::Ok(value) => value,
                Result::Err(_) => {
                    window.alert("Format", "Could not verify whether this partition is mounted.")
                    continue
                },
            }
            match mounted_partition {
                Option::Some(name) => {
                    let _ = warning.push_str("\nIt is mounted as ")
                    let _ = warning.push_str(name.as_str())
                    let _ = warning.push_str(": and will be reformatted.")
                },
                Option::None => {},
            }
            let _ = warning.push_str("\nAll data on it is lost. Proceed?")
            continue unless window.confirm("Format", warning.as_str())
            let volume_result = text_dialog(&window, "Volume name", "Volume name", partition.name(), 31)?
            match volume_result {
                Option::Some(chosen_volume) => match format_partition(disk.driver.as_str(), disk.unit, current, index, chosen_volume.as_str()) {
                    Result::Ok(_) => set_status(&window, "Status: partition formatted"),
                    Result::Err(error) => window.alert_error("Format", error),
                },
                Option::None => {},
            }
        }
        if action == FILE_SYSTEMS {
            let Option::Some(&current) = &model else { continue }
            match manage_file_systems(&window, &var file_system_pool, &drivers, current) {
                Result::Ok(true) => {
                    dirty = true
                    set_status(&window, "Status: filesystem pool changed; Save to apply")
                },
                Result::Ok(false) => {},
                Result::Err(_) => window.alert("Filesystems", "Could not open the filesystem manager."),
            }
        }

        if action == LOAD_DRIVER {
            let selected = choose_file(&window, "Select a device driver", "DEVS:")?
            let Option::Some(path) = selected else { continue }
            let driver = match load_driver(path.as_str()) {
                Result::Ok(value) => value,
                Result::Err(error) => {
                    window.alert_error("Driver", error)
                    continue
                },
            }
            let Result::Ok(driver_name) = FixedCString::<60>::new_from_str(driver.as_str()) else {
                window.alert("Driver", "The loaded device name is too long.")
                continue
            }
            selected_driver = add_driver(&var drivers, driver_name)
            refresh_driver_choices(&var window, &drivers, gadget_index(selected_driver))?
            disks = ArrayVec::<DiskEntry, 32>::new()
            refresh_unit_choices(&var window, &disks)?
            selected_disk = 0
            selected_partition = Option::None
            model = Option::None
            dirty = false
            clear_partitions(&var window)?
            set_status(&window, "Status: driver loaded; press Scan")
        }

        if action == SAVE {
            let Option::Some(&var current) = &var model else { continue }
            if save_model(&window, &disks, selected_disk, current, &file_system_pool) {
                dirty = false
                set_status(&window, "Status: saved and verified")
            }
        }
    }

    return Result::Ok(())
}

pub fn main() -> i32 {
    return run_scan() if command_line().trim().eq_ignore_case("scan")
    return match run() {
        Result::Ok(_) => 0,
        Result::Err(_) => 20,
    }
}
