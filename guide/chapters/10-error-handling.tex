\chapter{Error Handling}
\label{ch:error-handling}

\begin{quote}
\textit{``Errors should never pass silently. Unless explicitly silenced.''} --- The Zen of Python
\end{quote}

Novus treats errors as values, not exceptions. Every operation that can fail returns a \texttt{Result} type, making error handling explicit and impossible to ignore. This chapter covers Novus's error handling philosophy, the standard error types, and patterns for writing robust code.

% ============================================================================
\section{Philosophy: Errors as Values}
\label{sec:error-philosophy}
% ============================================================================

Unlike languages with exceptions, Novus requires explicit handling of errors:

\begin{itemize}
    \item \textbf{No hidden control flow} --- You always know when a function can fail
    \item \textbf{No uncaught exceptions} --- Errors can't silently propagate up the stack
    \item \textbf{Compile-time enforcement} --- Ignoring a \texttt{Result} produces a warning
    \item \textbf{Zero runtime cost} --- No exception tables or unwinding machinery
\end{itemize}

\subsection{Why Not Exceptions?}

Many languages use exceptions for error handling. While convenient in some ways, exceptions have significant drawbacks for systems programming:

\textbf{1. Stack Unwinding Cost:}
Exception handling requires runtime machinery to unwind the stack, search for handlers, and call destructors. On a 68020 running at 14 MHz, this overhead is unacceptable.

\textbf{2. Invisible Control Flow:}
Any function call might throw, making control flow analysis difficult. In Novus, functions that can fail are marked by their return type.

\textbf{3. Unpredictable Cleanup:}
With exceptions, you need \texttt{try/finally} blocks to ensure resources are released. In Novus, \texttt{Result} combinators and pattern matching make cleanup explicit.

\textbf{4. Binary Size:}
Exception tables and unwinding code add significant size to executables. Every byte counts on classic Amiga systems.

\subsection{Why Not errno?}

The traditional C approach of global error codes has its own problems:

\begin{lstlisting}[language=C]
// C with errno
FILE *f = fopen("data.txt", "r");
if (f == NULL) {
    if (errno == ENOENT) { /* not found */ }
    else if (errno == EACCES) { /* permission denied */ }
    // Easy to forget, hard to get right
}
\end{lstlisting}

\textbf{Problems with errno:}
\begin{itemize}
    \item Global mutable state is inherently error-prone
    \item Easy to forget to check it
    \item Can be overwritten by intermediate calls
    \item Not type-safe --- any integer could be assigned
    \item Thread-unsafe without special handling
\end{itemize}

AmigaOS uses a similar pattern with \texttt{IoErr()}:

\begin{lstlisting}[language=C]
// AmigaOS with IoErr()
BPTR lock = Lock("myfile", ACCESS_READ);
if (lock == 0) {
    LONG err = IoErr();  // Must call immediately!
    // But what if you call something else first?
    PrintFault(err, "Error");
}
\end{lstlisting}

The danger: if you call \emph{any} other DOS function before \texttt{IoErr()}, the error code may be overwritten.

\begin{comingfromc}
In C, errors are typically indicated by return values (\texttt{-1}, \texttt{NULL}) or global \texttt{errno}/\texttt{IoErr()}. This leads to bugs when callers forget to check.

In Novus, \texttt{Result<T, E>} forces you to handle both success and failure cases. You cannot accidentally use an error value as if it were successful.

\begin{center}
\begin{tabular}{ll}
\textbf{C (AmigaOS)} & \textbf{Novus} \\
\hline
\texttt{if (ptr == NULL)} & \texttt{match result \{ ... \}} \\
\texttt{if (fd < 0)} & \texttt{let file = open(path)?} \\
\texttt{IoErr()} & \texttt{Result::Err(DosError::NotFound)} \\
Manual conversion & \texttt{From} trait auto-conversion \\
\end{tabular}
\end{center}
\end{comingfromc}

% ============================================================================
\section{The Result Type}
\label{sec:result-type}
% ============================================================================

The core error handling type is \texttt{Result<T, E>}:

\begin{lstlisting}
enum Result<T, E> {
    Ok(T),    // Success with value
    Err(E),   // Failure with error
}
\end{lstlisting}

Every function that can fail returns a \texttt{Result}:

\begin{lstlisting}
fn parse_number(s: *u8) -> Result<i32, ParseError> {
    // Returns Ok(value) or Err(error)
}

fn open_file(path: *u8) -> Result<FileHandle, DosError> {
    // Returns Ok(handle) or Err(error)
}
\end{lstlisting}

\subsection{Result Methods Reference}

The \texttt{Result} type provides several methods for working with values:

\begin{center}
\begin{tabular}{lp{8cm}}
\textbf{Method} & \textbf{Description} \\
\hline
\texttt{is\_ok()} & Returns \texttt{true} if the result is \texttt{Ok} \\
\texttt{is\_err()} & Returns \texttt{true} if the result is \texttt{Err} \\
\texttt{unwrap()} & Extracts the \texttt{Ok} value, panics on \texttt{Err} \\
\texttt{unwrap\_or(default)} & Returns the \texttt{Ok} value or the provided default \\
\texttt{expect(msg)} & Like \texttt{unwrap()} but with a custom panic message \\
\texttt{ok()} & Converts to \texttt{Option<T>}, discarding the error \\
\texttt{err()} & Converts to \texttt{Option<E>}, discarding the success value \\
\end{tabular}
\end{center}

\begin{lstlisting}
let result: Result<i32, Error> = some_operation()

// Query the state
if result.is_ok() {
    println("Operation succeeded")
}

// Get value with default
let value = result.unwrap_or(0)

// Convert to Option
let maybe_value: Option<i32> = result.ok()
\end{lstlisting}

% ============================================================================
\section{The Option Type}
\label{sec:option-type}
% ============================================================================

While \texttt{Result} represents success or failure, \texttt{Option} represents presence or absence:

\begin{lstlisting}
enum Option<T> {
    Some(T),  // Value present
    None,     // No value
}
\end{lstlisting}

\subsection{When to Use Option vs Result}

\begin{itemize}
    \item Use \textbf{Option} when the only failure mode is ``not found'' or ``not available''
    \item Use \textbf{Result} when there are multiple failure modes to distinguish
    \item Use \textbf{Result} for operations that interact with the OS or hardware
\end{itemize}

\begin{lstlisting}
// Option: "not found" is the only failure
fn find_user(id: u32) -> Option<User> { ... }

// Result: multiple failure modes possible
fn load_user(id: u32) -> Result<User, DbError> { ... }
\end{lstlisting}

\subsection{Option Methods Reference}

\begin{center}
\begin{tabular}{lp{8cm}}
\textbf{Method} & \textbf{Description} \\
\hline
\texttt{is\_some()} & Returns \texttt{true} if value is present \\
\texttt{is\_none()} & Returns \texttt{true} if no value \\
\texttt{unwrap()} & Extracts the value, panics on \texttt{None} \\
\texttt{unwrap\_or(default)} & Returns value or provided default \\
\texttt{expect(msg)} & Like \texttt{unwrap()} with custom panic message \\
\end{tabular}
\end{center}

\subsection{Converting Between Option and Result}

Sometimes you need to convert between the two types:

\begin{lstlisting}
// Option -> Result: provide an error for None
fn get_config_value(key: *u8) -> Result<*u8, ConfigError> {
    match config.get(key) {
        Option::Some(val) => Result::Ok(val),
        Option::None => Result::Err(ConfigError::MissingKey)
    }
}
\end{lstlisting}

\begin{comingfromc}
In C, null pointers serve double duty: representing ``no value'' (Option) and ``error occurred'' (Result). This conflation causes bugs.

\begin{lstlisting}[language=C]
// C: Is NULL "not found" or "error"?
Node *node = find_node(list, key);
if (node == NULL) {
    // Did find fail, or is the key just missing?
    // Have to check errno to know...
}
\end{lstlisting}

In Novus, the type system distinguishes these cases. \texttt{Option<T>} means ``might not exist'' while \texttt{Result<T, E>} means ``might have failed''.
\end{comingfromc}

% ============================================================================
\section{The ? Operator In Depth}
\label{sec:question-operator}
% ============================================================================

The \texttt{?} operator is syntactic sugar for early return on error. It transforms verbose error checking into concise, readable code.

\subsection{What ? Actually Does}

When you write:
\begin{lstlisting}
let file = open_file(path)?
\end{lstlisting}

The compiler generates equivalent code to:
\begin{lstlisting}
let file = match open_file(path) {
    Result::Ok(val) => val,
    Result::Err(e) => return Result::Err(e)
}
\end{lstlisting}

The \texttt{?} operator:
\begin{enumerate}
    \item Checks if the result is \texttt{Err}
    \item If \texttt{Err}, returns immediately with that error
    \item If \texttt{Ok}, unwraps the value and continues
\end{enumerate}

\subsection{Chaining Multiple ? Calls}

The real power of \texttt{?} shows when chaining fallible operations:

\begin{lstlisting}
fn process_file(path: *u8) -> Result<Data, DosError> {
    let file = open_file(path)?       // Returns Err early if failed
    let header = read_header(&file)?  // Returns Err early if failed
    let body = read_body(&file)?      // Returns Err early if failed
    let data = parse_data(&body)?     // Returns Err early if failed
    Result::Ok(data)
}
\end{lstlisting}

Without \texttt{?}, this would require deeply nested match statements:

\begin{lstlisting}
fn process_file(path: *u8) -> Result<Data, DosError> {
    match open_file(path) {
        Result::Ok(file) => {
            match read_header(&file) {
                Result::Ok(header) => {
                    match read_body(&file) {
                        Result::Ok(body) => {
                            match parse_data(&body) {
                                Result::Ok(data) => Result::Ok(data),
                                Result::Err(e) => Result::Err(e)
                            }
                        },
                        Result::Err(e) => Result::Err(e)
                    }
                },
                Result::Err(e) => Result::Err(e)
            }
        },
        Result::Err(e) => Result::Err(e)
    }
}
\end{lstlisting}

\subsection{Using ? in main()}

In Novus, \texttt{main()} returns \texttt{i32} (the exit code). You can still use \texttt{?} by wrapping your logic:

\begin{lstlisting}
fn run() -> Result<(), AppError> {
    let config = load_config()?
    let window = open_window()?
    main_loop(&window)?
    Result::Ok(())
}

pub fn main() -> i32 {
    match run() {
        Result::Ok(_) => return 0,
        Result::Err(err) => {
            println(err.message())
            return 1
        }
    }
}
\end{lstlisting}

\subsection{Limitations of ?}

The \texttt{?} operator can only be used in functions that return \texttt{Result}:

\begin{lstlisting}
fn valid() -> Result<(), Error> {
    let x = fallible_op()?  // OK: function returns Result
    Result::Ok(())
}

fn invalid() {
    let x = fallible_op()?  // ERROR: function doesn't return Result
}
\end{lstlisting}

% ============================================================================
\section{Standard Error Types}
\label{sec:standard-errors}
% ============================================================================

Novus provides error types for each AmigaOS subsystem.

\subsection{DosError}

Errors from the DOS library (file operations, paths):

\begin{lstlisting}
pub enum DosError {
    NotFound,           // File/directory not found (IoErr 205)
    AlreadyExists,      // Object already exists (IoErr 203)
    DirNotFound,        // Directory not found (IoErr 212)
    DirectoryNotEmpty,  // Can't delete non-empty dir (IoErr 216)
    PermissionDenied,   // Access denied (IoErr 214)
    WriteProtected,     // Disk is write-protected (IoErr 223)
    ReadProtected,      // File is read-protected (IoErr 224)
    DiskFull,           // No space on disk (IoErr 221)
    NoDisk,             // No disk in drive (IoErr 218)
    DeviceNotMounted,   // Device not available (IoErr 225)
    InvalidLock,        // Bad file lock (IoErr 211)
    Unknown,            // Unmapped error code
}
\end{lstlisting}

\begin{center}
\begin{tabular}{lll}
\textbf{DosError Variant} & \textbf{IoErr() Code} & \textbf{Description} \\
\hline
\texttt{NotFound} & 205 & Object not found \\
\texttt{AlreadyExists} & 203 & Object already exists \\
\texttt{DirNotFound} & 212 & Directory not found \\
\texttt{DirectoryNotEmpty} & 216 & Directory not empty \\
\texttt{PermissionDenied} & 214 & Object is protected \\
\texttt{WriteProtected} & 223 & Disk is write-protected \\
\texttt{ReadProtected} & 224 & File is read-protected \\
\texttt{DiskFull} & 221 & Disk is full \\
\texttt{NoDisk} & 218 & No disk in drive \\
\texttt{DeviceNotMounted} & 225 & Device not mounted \\
\texttt{InvalidLock} & 211 & Invalid lock \\
\end{tabular}
\end{center}

\subsection{ExecError}

Errors from the Exec library (memory, tasks, signals):

\begin{lstlisting}
pub enum ExecError {
    NoMem,              // Memory allocation failed
    BadSignal,          // Invalid signal number
    NullReturn,         // Unexpected null return
    BadTask,            // Invalid task pointer
    BadPort,            // Invalid message port
    SignalInUse,        // Signal already allocated
    TaskNotFound,       // FindTask failed
    LibraryNotFound,    // OpenLibrary failed
    IndexOutOfBounds,   // Array bounds violation
    CapacityExceeded,   // Fixed buffer full
}
\end{lstlisting}

\subsection{IntuitionError}

Errors from the Intuition library (windows, screens, gadgets):

\begin{lstlisting}
pub enum IntuitionError {
    WindowOpenFailed,     // OpenWindow failed
    ScreenOpenFailed,     // OpenScreen failed
    GadgetCreateFailed,   // Gadget creation failed
    MenuCreateFailed,     // Menu creation failed
    InvalidWindow,        // Bad window pointer
    InvalidScreen,        // Bad screen pointer
    NoIDCMP,              // IDCMP setup failed
    BadDisplayMode,       // Invalid display mode
}
\end{lstlisting}

\subsection{GraphicsError}

Errors from the Graphics library (bitmaps, sprites, fonts):

\begin{lstlisting}
pub enum GraphicsError {
    BitMapAllocFailed,    // BitMap allocation failed
    FontOpenFailed,       // OpenFont failed
    InvalidBitMap,        // Bad BitMap pointer
    InvalidRastPort,      // Bad RastPort pointer
    InvalidSpriteChannel, // Bad sprite channel (0-7)
    SpriteChannelInUse,   // Channel already allocated
    NoSpritesAvailable,   // All sprites in use
}
\end{lstlisting}

% ============================================================================
\section{The NovusError Wrapper}
\label{sec:novus-error}
% ============================================================================

When a function may fail with errors from multiple subsystems, use \texttt{NovusError}:

\begin{lstlisting}
pub enum NovusError {
    Dos(DosError),
    Exec(ExecError),
    Intuition(IntuitionError),
    Graphics(GraphicsError),
}
\end{lstlisting}

The \texttt{From} trait implementations enable automatic conversion with the \texttt{?} operator:

\begin{lstlisting}
fn load_and_display(path: *u8) -> Result<(), NovusError> {
    let data = read_file(path)?       // DosError -> NovusError
    let screen = open_screen()?       // IntuitionError -> NovusError
    let bitmap = load_bitmap(&data)?  // GraphicsError -> NovusError
    display(screen, bitmap)
    Result::Ok(())
}
\end{lstlisting}

% ============================================================================
\section{Error Conversion with From}
\label{sec:error-conversion}
% ============================================================================

The \texttt{From} trait enables automatic error type conversion when using \texttt{?}:

\begin{lstlisting}
trait From<T> {
    fn convert(value: T) -> Self
}
\end{lstlisting}

When you use \texttt{?} in a function returning \texttt{Result<T, NovusError>}, and the inner call returns \texttt{Result<T, DosError>}, the compiler automatically calls \texttt{NovusError::From<DosError>::convert()} to wrap the error.

\subsection{Manual Conversion}

You can also convert errors manually:

\begin{lstlisting}
from std::error::errors import novus_error_from_dos

fn wrapper() -> Result<(), NovusError> {
    let file = match open("file.txt", MODE_OLDFILE) {
        Result::Ok(f) => f,
        Result::Err(e) => return Result::Err(novus_error_from_dos(e))
    }
    Result::Ok(())
}
\end{lstlisting}

\subsection{Getting Raw Error Codes}

For debugging or interop with C code, get raw AmigaOS error codes:

\begin{lstlisting}
from std::error::errors import dos_error_to_code

let code = dos_error_to_code(DosError::NotFound)  // Returns 205
\end{lstlisting}

% ============================================================================
\section{Error Messages}
\label{sec:error-messages}
% ============================================================================

All standard error types implement a \texttt{message()} method:

\begin{lstlisting}
match result {
    Result::Err(err) => {
        print("Error: ")
        println(err.message())
    },
    _ => {}
}
\end{lstlisting}

Error messages are human-readable strings:
\begin{itemize}
    \item \texttt{DosError::NotFound} $\rightarrow$ \texttt{"Object not found"}
    \item \texttt{ExecError::NoMem} $\rightarrow$ \texttt{"Memory allocation failed"}
    \item \texttt{IntuitionError::WindowOpenFailed} $\rightarrow$ \texttt{"Failed to open window"}
\end{itemize}

% ============================================================================
\section{Error Handling Patterns}
\label{sec:error-patterns}
% ============================================================================

This section covers common patterns for handling errors effectively.

\subsection{Early Return Pattern}

Validate inputs at the start of a function and return errors immediately. This keeps the main logic clean:

\begin{lstlisting}
fn process(data: *u8, len: u32) -> Result<(), Error> {
    // Validate all inputs first
    if data == null {
        return Result::Err(Error::InvalidInput)
    }
    if len == 0 {
        return Result::Err(Error::EmptyData)
    }
    if len > MAX_SIZE {
        return Result::Err(Error::TooLarge)
    }

    // All validation passed - main logic here
    do_actual_work(data, len)
}
\end{lstlisting}

\begin{comingfromc}
In C, you often see scattered validation throughout functions:

\begin{lstlisting}[language=C]
int process(char *data, size_t len) {
    int result = init_something();
    if (result < 0) return result;

    if (data == NULL) return -EINVAL;  // Mixed with logic!

    result = do_more_work();
    if (result < 0) goto cleanup;

    if (len == 0) goto cleanup;  // Validation too late!
    // ...
}
\end{lstlisting}

The early return pattern in Novus collects all validation at the top, making the ``happy path'' obvious.
\end{comingfromc}

\subsection{Fallback Chains}

Try multiple approaches, falling back on failure. This is common for finding configuration files or resources:

\begin{lstlisting}
fn find_config() -> Result<Config, ConfigError> {
    // Try user config first
    match load_from("ENV:app.cfg") {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(_) => {}
    }

    // Fall back to archive
    match load_from("ENVARC:app.cfg") {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(_) => {}
    }

    // Fall back to system config
    match load_from("S:app.cfg") {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(_) => {}
    }

    // Use defaults as last resort
    Result::Ok(default_config())
}
\end{lstlisting}

\subsection{Cleanup on Error with defer}

Use \texttt{defer} to ensure cleanup even when returning early on error:

\begin{lstlisting}
fn process_file(path: *u8) -> Result<Data, DosError> {
    let file = open(path, MODE_OLDFILE)?
    defer { close(file) }  // Always runs, even on error return

    let header = read_header(&file)?  // May return early
    let body = read_body(&file)?      // May return early

    Result::Ok(combine(header, body))
    // defer runs here on success too
}
\end{lstlisting}

\subsection{Error Context}

Add context when propagating errors by matching and re-wrapping with domain-specific information:

\begin{lstlisting}
fn load_user_config() -> Result<Config, ConfigError> {
    let path = get_config_path()?

    let file = match open(path, MODE_OLDFILE) {
        Result::Ok(f) => f,
        Result::Err(_) => return Result::Err(ConfigError::FileNotFound)
    }

    let data = match read_all(&file) {
        Result::Ok(d) => d,
        Result::Err(_) => return Result::Err(ConfigError::ReadFailed)
    }

    parse_config(&data)
}
\end{lstlisting}

\subsection{Retry Pattern}

For transient failures (disk not ready, network timeout), retry a few times:

\begin{lstlisting}
fn open_with_retry(path: *u8, max_retries: u32) -> Result<FileHandle, DosError> {
    var attempts: u32 = 0

    while attempts < max_retries {
        match open(path, MODE_OLDFILE) {
            Result::Ok(file) => return Result::Ok(file),
            Result::Err(DosError::NoDisk) => {
                // Disk not ready - wait and retry
                delay(Duration::from_secs(1))
                attempts = attempts + 1
            },
            Result::Err(e) => return Result::Err(e)  // Non-retriable
        }
    }

    Result::Err(DosError::NoDisk)
}
\end{lstlisting}

% ============================================================================
\section{Panic: When Errors Are Bugs}
\label{sec:panic}
% ============================================================================

A \textbf{panic} is for situations where continuing is impossible---invariant violations, impossible states, or programmer errors.

\begin{lstlisting}
// Panic on invariant violation
fn get_first(vec: &Vec<i32>) -> i32 {
    if vec.len() == 0 {
        panic!("get_first called on empty vector")
    }
    return vec[0].unwrap()
}
\end{lstlisting}

\subsection{Panic vs Error}

\begin{center}
\begin{tabular}{lll}
\textbf{Use} & \textbf{When} & \textbf{Example} \\
\hline
\texttt{Result::Err} & Failure is expected & File not found \\
\texttt{panic!()} & Failure is a bug & Index out of bounds \\
\end{tabular}
\end{center}

\subsection{assert!() for Debug Checks}

Use \texttt{assert!()} to catch programming errors during development:

\begin{lstlisting}
fn process_buffer(buf: *u8, len: u32) {
    assert!(buf != null, "buffer must not be null")
    assert!(len > 0, "length must be positive")
    // ...
}
\end{lstlisting}

Assertions are checked in debug builds. They verify invariants that should always hold if the code is correct.

% ============================================================================
\section{Creating Custom Errors}
\label{sec:custom-errors}
% ============================================================================

Define domain-specific error types for your application:

\begin{lstlisting}
pub enum ConfigError {
    /// Config file not found in any search location
    FileNotFound,

    /// File exists but cannot be read
    ReadFailed(DosError),

    /// File is too large to load
    FileTooLarge(u32),

    /// Syntax error at specific line
    ParseError(u32),

    /// Required key is missing
    MissingKey,

    /// Value is invalid for expected type
    InvalidValue,

    /// Memory allocation failed
    OutOfMemory,
}
\end{lstlisting}

\subsection{Implementing message()}

Provide human-readable messages:

\begin{lstlisting}
impl ConfigError {
    pub fn message(&self) -> *u8 {
        match *self {
            ConfigError::FileNotFound =>
                return "Config file not found in any search location",
            ConfigError::ReadFailed(_) =>
                return "Failed to read config file",
            ConfigError::FileTooLarge(_) =>
                return "Config file exceeds maximum size",
            ConfigError::ParseError(_) =>
                return "Syntax error in config file",
            ConfigError::MissingKey =>
                return "Required configuration key is missing",
            ConfigError::InvalidValue =>
                return "Configuration value is invalid",
            ConfigError::OutOfMemory =>
                return "Out of memory loading config",
        }
    }
}
\end{lstlisting}

\subsection{Extracting Error Details}

For errors with associated data, provide accessor methods:

\begin{lstlisting}
impl ConfigError {
    /// Get the line number for parse errors
    pub fn line_number(&self) -> Option<u32> {
        match *self {
            ConfigError::ParseError(line) => Option::Some(line),
            _ => Option::None
        }
    }

    /// Get the file size for too-large errors
    pub fn file_size(&self) -> Option<u32> {
        match *self {
            ConfigError::FileTooLarge(size) => Option::Some(size),
            _ => Option::None
        }
    }
}
\end{lstlisting}

% ============================================================================
\section{Showcase: Config Loader}
\label{sec:showcase-config-loader}
% ============================================================================

This complete program demonstrates comprehensive error handling in Novus. It loads configuration files with fallback locations, custom error types, and proper error reporting.

\begin{lstlisting}
// config_loader.novus - Robust Config Loader

from std::core import Option, Result
from std::error::errors import DosError
from amiga::raw::dos import Open, Close, Read, Lock, UnLock, Examine
from amiga::raw::consts import MODE_OLDFILE, ACCESS_READ
from std::io::file import print, println

// ----------------------------------------------------------------------------
// Custom Error Type with Rich Information
// ----------------------------------------------------------------------------

pub enum ConfigError {
    FileNotFound,
    ReadFailed(DosError),
    FileTooLarge(u32),
    ParseError(u32),  // Line number
    MissingKey,
    InvalidValue,
    OutOfMemory,
}

impl ConfigError {
    pub fn message(&self) -> *u8 {
        match *self {
            ConfigError::FileNotFound =>
                return "Config file not found in any search location",
            ConfigError::ReadFailed(_) =>
                return "Failed to read config file",
            ConfigError::FileTooLarge(_) =>
                return "Config file exceeds maximum size (64KB)",
            ConfigError::ParseError(_) =>
                return "Syntax error in config file",
            ConfigError::MissingKey =>
                return "Required configuration key is missing",
            ConfigError::InvalidValue =>
                return "Configuration value is invalid",
            ConfigError::OutOfMemory =>
                return "Out of memory loading config",
        }
    }

    pub fn line_number(&self) -> Option<u32> {
        match *self {
            ConfigError::ParseError(line) => Option::Some(line),
            _ => Option::None
        }
    }
}

// ----------------------------------------------------------------------------
// Fallback Chain: Try Multiple Locations
// ----------------------------------------------------------------------------

pub fn load_config(name: *u8) -> Result<Config, ConfigError> {
    var path_buffer: [u8] = [0; 256]

    // Try ENV: first (volatile)
    copy_path(&path_buffer[0], "ENV:", name)
    match try_load_from(&path_buffer[0]) {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(_) => {}
    }

    // Try ENVARC: (persistent)
    copy_path(&path_buffer[0], "ENVARC:", name)
    match try_load_from(&path_buffer[0]) {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(_) => {}
    }

    // Try S: (startup scripts)
    copy_path(&path_buffer[0], "S:", name)
    match try_load_from(&path_buffer[0]) {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(_) => {}
    }

    // Try current directory
    match try_load_from(name) {
        Result::Ok(cfg) => return Result::Ok(cfg),
        Result::Err(e) => return Result::Err(e)
    }
}

// ----------------------------------------------------------------------------
// Main: Demonstrating Error Handling
// ----------------------------------------------------------------------------

pub fn main() -> i32 {
    println("Config Loader - Error Handling Showcase")
    println("")

    match load_config("test.config") {
        Result::Ok(config) => {
            println("Config loaded successfully!")
            print("  Source: ")
            println(&config.source_path[0])
            return 0
        },
        Result::Err(err) => {
            print("Error: ")
            println(err.message())
            return 1
        }
    }
}
\end{lstlisting}

This program demonstrates:
\begin{itemize}
    \item \textbf{Custom error types} with variant-specific data (line numbers, file sizes)
    \item \textbf{Fallback chains} trying multiple locations (ENV:, ENVARC:, S:, current dir)
    \item \textbf{Error messages} via the \texttt{message()} method
    \item \textbf{Error context} extraction via \texttt{line\_number()}
\end{itemize}

The full source code is available in \texttt{Novus.Tests/Examples/config\_loader\_showcase.novus}.

% ============================================================================
\section{Summary}
\label{sec:error-summary}
% ============================================================================

\begin{itemize}
    \item \textbf{Use \texttt{Result<T, E>}} for all fallible operations
    \item \textbf{Use \texttt{Option<T>}} when ``not found'' is the only failure mode
    \item \textbf{Pattern match} for explicit handling of all cases
    \item \textbf{Use \texttt{?}} to propagate errors concisely
    \item \textbf{Use \texttt{unwrap\_or}} for safe defaults
    \item \textbf{Avoid \texttt{unwrap()}} except when failure is impossible
    \item \textbf{Use standard errors} (DosError, ExecError, etc.) for AmigaOS operations
    \item \textbf{Use NovusError} when crossing subsystem boundaries
    \item \textbf{Use \texttt{defer}} to ensure cleanup on error paths
    \item \textbf{Use panic} only for invariant violations and bugs
    \item \textbf{Create custom errors} for domain-specific failure modes
\end{itemize}

Error handling in Novus is explicit but ergonomic. The \texttt{?} operator and \texttt{Result} combinators make it easy to write code that correctly handles failures without obscuring the happy path.

% ============================================================================
\section{Advanced Error Patterns}
\label{sec:advanced-error-patterns}
% ============================================================================

This section covers advanced patterns for complex error handling scenarios.

\subsection{Collecting Multiple Errors}

Sometimes you want to validate everything before reporting, collecting all errors rather than stopping at the first:

\begin{lstlisting}
pub struct ValidationErrors {
    errors: Vec<*u8>,
}

impl ValidationErrors {
    pub fn new() -> ValidationErrors {
        return ValidationErrors { errors: Vec::new() }
    }

    pub fn add(&var self, message: *u8) {
        self.errors.push(message)
    }

    pub fn has_errors(&self) -> bool {
        return self.errors.len() > 0
    }

    pub fn to_result(&self) -> Result<(), ValidationError> {
        if self.has_errors() {
            return Result::Err(ValidationError::Multiple(self.errors.len()))
        }
        Result::Ok(())
    }
}

fn validate_user(user: &User) -> Result<(), ValidationError> {
    var errors = ValidationErrors::new()

    if user.name.len() == 0 {
        errors.add("Name is required")
    }
    if user.age < 0 {
        errors.add("Age must be non-negative")
    }
    if user.email.len() == 0 {
        errors.add("Email is required")
    }
    if !user.email.contains('@') {
        errors.add("Email must contain @")
    }

    // Print all errors
    for i in 0..errors.errors.len() {
        print("  - ")
        println(errors.errors[i])
    }

    errors.to_result()
}
\end{lstlisting}

\subsection{Error Recovery Strategies}

Different errors require different recovery strategies. Here's a pattern that encapsulates recovery logic:

\begin{lstlisting}
pub enum RecoveryAction {
    Retry,        // Try the operation again
    Skip,         // Skip this item, continue with others
    Fallback,     // Use a fallback value or method
    Abort,        // Stop completely
}

fn determine_recovery(err: &DosError) -> RecoveryAction {
    match *err {
        // Transient errors - retry makes sense
        DosError::NoDisk => RecoveryAction::Retry,

        // Missing files - skip or use fallback
        DosError::NotFound => RecoveryAction::Fallback,

        // Permission errors - cannot recover
        DosError::PermissionDenied => RecoveryAction::Abort,

        // Other errors - skip by default
        _ => RecoveryAction::Skip
    }
}

fn process_files(paths: &[*u8]) -> Result<u32, DosError> {
    var processed: u32 = 0

    for i in 0..paths.len() {
        let path = paths[i]

        loop {
            match open(path, MODE_OLDFILE) {
                Result::Ok(file) => {
                    defer { close(file) }
                    process_file(&file)?
                    processed += 1
                    break
                },
                Result::Err(err) => {
                    match determine_recovery(&err) {
                        RecoveryAction::Retry => {
                            delay(Duration::from_millis(500))
                            continue
                        },
                        RecoveryAction::Skip => break,
                        RecoveryAction::Fallback => {
                            use_fallback(path)
                            processed += 1
                            break
                        },
                        RecoveryAction::Abort => return Result::Err(err)
                    }
                }
            }
        }
    }

    Result::Ok(processed)
}
\end{lstlisting}

\subsection{Error Logging Pattern}

For production code, logging errors before propagating them helps debugging:

\begin{lstlisting}
fn log_error(location: *u8, err: &NovusError) {
    print("[ERROR] ")
    print(location)
    print(": ")
    println(err.message())
}

fn load_with_logging(path: *u8) -> Result<Data, NovusError> {
    let file = match open(path, MODE_OLDFILE) {
        Result::Ok(f) => f,
        Result::Err(e) => {
            let err = novus_error_from_dos(e)
            log_error("load_with_logging:open", &err)
            return Result::Err(err)
        }
    }
    defer { close(file) }

    let data = match read_all(&file) {
        Result::Ok(d) => d,
        Result::Err(e) => {
            let err = novus_error_from_dos(e)
            log_error("load_with_logging:read", &err)
            return Result::Err(err)
        }
    }

    Result::Ok(data)
}
\end{lstlisting}

\begin{comingfromc}
C programs often use preprocessor macros for error logging:

\begin{lstlisting}[language=C]
#define CHECK_ERROR(expr, msg) \
    do { \
        if (!(expr)) { \
            fprintf(stderr, "%s:%d: %s\n", __FILE__, __LINE__, msg); \
            return -1; \
        } \
    } while(0)
\end{lstlisting}

In Novus, logging is explicit and type-safe. You can create helper functions that log and return errors in a single call, but the pattern remains clear and debuggable.
\end{comingfromc}

\subsection{Error Wrapping for Context}

Add high-level context to low-level errors:

\begin{lstlisting}
pub enum AppError {
    ConfigLoad(ConfigError),
    DatabaseConnect(DbError),
    NetworkTimeout(NetworkError),
    Initialization(*u8),  // Free-form context message
}

impl AppError {
    pub fn message(&self) -> *u8 {
        match *self {
            AppError::ConfigLoad(_) => "Failed to load configuration",
            AppError::DatabaseConnect(_) => "Failed to connect to database",
            AppError::NetworkTimeout(_) => "Network operation timed out",
            AppError::Initialization(msg) => msg,
        }
    }

    pub fn inner(&self) -> Option<*u8> {
        match *self {
            AppError::ConfigLoad(ref e) => Option::Some(e.message()),
            AppError::DatabaseConnect(ref e) => Option::Some(e.message()),
            AppError::NetworkTimeout(ref e) => Option::Some(e.message()),
            AppError::Initialization(_) => Option::None,
        }
    }
}

fn init_app() -> Result<App, AppError> {
    // Wrap errors with context
    let config = load_config("app.cfg")
        .map_err(|e| AppError::ConfigLoad(e))?

    let db = connect_database(&config.db_path)
        .map_err(|e| AppError::DatabaseConnect(e))?

    Result::Ok(App { config, db })
}

// Display both layers
fn report_error(err: &AppError) {
    print("Error: ")
    println(err.message())

    match err.inner() {
        Option::Some(inner) => {
            print("  Cause: ")
            println(inner)
        },
        Option::None => {}
    }
}
\end{lstlisting}

% ============================================================================
\section{Testing Error Conditions}
\label{sec:testing-errors}
% ============================================================================

Testing that your code handles errors correctly is just as important as testing the happy path.

\subsection{Triggering Specific Errors}

Create test helpers that return specific errors:

\begin{lstlisting}
#[test]
fn test_handles_not_found() {
    // Use a path that definitely doesn't exist
    let result = load_config("NONEXISTENT:fake.cfg")

    match result {
        Result::Err(ConfigError::FileNotFound) => {
            // Expected - test passes
        },
        Result::Ok(_) => {
            panic!("Expected FileNotFound, got Ok")
        },
        Result::Err(e) => {
            panic!("Expected FileNotFound, got different error")
        }
    }
}

#[test]
fn test_handles_parse_error() {
    // Create a file with invalid content
    let file = create_test_file("test_bad.cfg", "invalid { content")
    defer { delete_test_file("test_bad.cfg") }

    let result = parse_config(&file)

    match result {
        Result::Err(ConfigError::ParseError(line)) => {
            assert!(line == 1, "Error should be on line 1")
        },
        _ => panic!("Expected ParseError")
    }
}
\end{lstlisting}

\subsection{Error Injection with Function Parameters}

Design functions to accept fallible operations as parameters:

\begin{lstlisting}
// Production signature
type FileOpener = fn(*u8) -> Result<FileHandle, DosError>

// Use dependency injection for testing
fn process_with_opener(
    path: *u8,
    opener: FileOpener
) -> Result<Data, DosError> {
    let file = opener(path)?
    defer { close(file) }
    read_and_parse(&file)
}

// In tests, provide a mock opener
#[test]
fn test_handles_open_failure() {
    fn failing_opener(path: *u8) -> Result<FileHandle, DosError> {
        Result::Err(DosError::PermissionDenied)
    }

    let result = process_with_opener("test.txt", failing_opener)

    match result {
        Result::Err(DosError::PermissionDenied) => { /* pass */ },
        _ => panic!("Expected PermissionDenied")
    }
}
\end{lstlisting}

\subsection{Testing Error Messages}

Verify that error messages are correct:

\begin{lstlisting}
#[test]
fn test_error_messages() {
    assert_eq!(
        DosError::NotFound.message(),
        "Object not found"
    )
    assert_eq!(
        DosError::DiskFull.message(),
        "Disk is full"
    )
    assert_eq!(
        ConfigError::MissingKey.message(),
        "Required configuration key is missing"
    )
}
\end{lstlisting}

% ============================================================================
\section{Performance Considerations}
\label{sec:error-performance}
% ============================================================================

Error handling in Novus is designed to be zero-cost when errors don't occur.

\subsection{Result is Zero-Cost}

The \texttt{Result<T, E>} type is a tagged union. When \texttt{T} and \texttt{E} have the same size, \texttt{Result} is just \texttt{sizeof(T) + 1} byte for the discriminant (often padded to alignment). There's no heap allocation, no vtable, no exception tables.

\begin{lstlisting}
// Result<i32, i32> is exactly 8 bytes:
// - 4 bytes for the value (i32)
// - 4 bytes for alignment (includes discriminant)

// Result<*u8, DosError> is 8 bytes:
// - 4 bytes for the pointer or error code
// - 4 bytes for alignment

// Compare to C++ exceptions:
// - Exception tables in every function
// - RTTI for type matching
// - Stack unwinding machinery
\end{lstlisting}

\subsection{The Cost of the ? Operator}

The \texttt{?} operator compiles to a simple conditional branch:

\begin{lstlisting}
// This Novus code:
let value = operation()?

// Compiles to roughly this 68k:
//     move.l  d0,d2           ; save result
//     tst.l   d0              ; check discriminant
//     bne.s   .error          ; branch if error
//     move.l  4(sp),d0        ; extract value
//     bra.s   .continue
// .error:
//     move.l  d2,d0           ; return error
//     rts
// .continue:
\end{lstlisting}

This is essentially the same cost as checking a return value in C.

\subsection{Avoid Unwrap in Hot Paths}

While \texttt{unwrap()} is convenient, it has overhead from the panic path:

\begin{lstlisting}
// Slower - has panic machinery
fn sum_values(results: &[Result<i32, Error>]) -> i32 {
    var total: i32 = 0
    for i in 0..results.len() {
        total += results[i].unwrap()  // Panic check each iteration
    }
    return total
}

// Faster - single error check
fn sum_values_checked(results: &[Result<i32, Error>]) -> Result<i32, Error> {
    var total: i32 = 0
    for i in 0..results.len() {
        total += results[i]?  // Early return, no panic code
    }
    Result::Ok(total)
}
\end{lstlisting}

\subsection{When to Use unwrap\_or}

For performance-critical code with known-safe defaults, \texttt{unwrap\_or} avoids branch misprediction:

\begin{lstlisting}
// For optional lookups with a sensible default
fn get_color(palette: &Palette, index: u32) -> u32 {
    // Returns black (0) if index is out of range
    return palette.get(index).unwrap_or(0)
}

// This compiles to efficient branchless code on 68020+
\end{lstlisting}

% ============================================================================
\section{Real-World Error Handling}
\label{sec:real-world-errors}
% ============================================================================

This section shows complete error handling for common Amiga scenarios.

\subsection{File Processing Pipeline}

A complete file processing example with proper cleanup:

\begin{lstlisting}
pub fn convert_file(
    input_path: *u8,
    output_path: *u8
) -> Result<u32, ConvertError> {
    // Open input file
    let input = match open(input_path, MODE_OLDFILE) {
        Result::Ok(f) => f,
        Result::Err(e) => return Result::Err(ConvertError::InputOpen(e))
    }
    defer { close(input) }

    // Get file size for progress reporting
    let size = match examine(&input) {
        Result::Ok(info) => info.size,
        Result::Err(e) => return Result::Err(ConvertError::InputRead(e))
    }

    // Open output file
    let output = match open(output_path, MODE_NEWFILE) {
        Result::Ok(f) => f,
        Result::Err(e) => return Result::Err(ConvertError::OutputOpen(e))
    }
    defer {
        close(output)
        // Delete partial output on error (defers run in reverse order)
        if error_occurred {
            delete_file(output_path)
        }
    }

    var error_occurred = false
    var bytes_written: u32 = 0

    // Process in chunks
    var buffer: [u8] = [0; 4096]
    loop {
        let bytes_read = match read(&input, &buffer) {
            Result::Ok(0) => break,  // EOF
            Result::Ok(n) => n,
            Result::Err(e) => {
                error_occurred = true
                return Result::Err(ConvertError::InputRead(e))
            }
        }

        let converted = convert_chunk(&buffer, bytes_read)?

        match write(&output, &converted, converted.len()) {
            Result::Ok(n) => bytes_written += n,
            Result::Err(e) => {
                error_occurred = true
                return Result::Err(ConvertError::OutputWrite(e))
            }
        }
    }

    Result::Ok(bytes_written)
}
\end{lstlisting}

\subsection{GUI Error Feedback}

Reporting errors through Intuition requesters:

\begin{lstlisting}
pub fn show_error(window: *Window, err: &NovusError) {
    var body = EasyStruct {
        struct_size: size_of::<EasyStruct>(),
        flags: 0,
        title: "Error",
        text_format: err.message(),
        gadget_format: "OK",
    }

    EasyRequest(window, &body, null, null)
}

pub fn show_error_with_retry(
    window: *Window,
    err: &NovusError
) -> bool {
    var body = EasyStruct {
        struct_size: size_of::<EasyStruct>(),
        flags: 0,
        title: "Error",
        text_format: err.message(),
        gadget_format: "Retry|Cancel",
    }

    // Returns 1 for Retry, 0 for Cancel
    return EasyRequest(window, &body, null, null) == 1
}

// Usage with retry loop
fn load_with_ui_retry(
    window: *Window,
    path: *u8
) -> Result<Data, NovusError> {
    loop {
        match load_data(path) {
            Result::Ok(data) => return Result::Ok(data),
            Result::Err(err) => {
                if !show_error_with_retry(window, &err) {
                    return Result::Err(err)
                }
                // User chose Retry - loop continues
            }
        }
    }
}
\end{lstlisting}

\subsection{Multi-Step Initialization}

Initializing multiple subsystems with proper cleanup on partial failure:

\begin{lstlisting}
pub struct Application {
    screen: *Screen,
    window: *Window,
    audio: AudioContext,
    font: *TextFont,
}

impl Application {
    pub fn init() -> Result<Application, AppError> {
        // Step 1: Open screen
        let screen = match open_screen(&SCREEN_SPEC) {
            Result::Ok(s) => s,
            Result::Err(e) => return Result::Err(AppError::Screen(e))
        }

        // Step 2: Open window (cleanup screen on failure)
        let window = match open_window(screen, &WINDOW_SPEC) {
            Result::Ok(w) => w,
            Result::Err(e) => {
                close_screen(screen)
                return Result::Err(AppError::Window(e))
            }
        }

        // Step 3: Initialize audio (cleanup window and screen on failure)
        let audio = match AudioContext::init() {
            Result::Ok(a) => a,
            Result::Err(e) => {
                close_window(window)
                close_screen(screen)
                return Result::Err(AppError::Audio(e))
            }
        }

        // Step 4: Open font (cleanup all previous on failure)
        let font = match open_font("topaz.font", 8) {
            Result::Ok(f) => f,
            Result::Err(e) => {
                audio.shutdown()
                close_window(window)
                close_screen(screen)
                return Result::Err(AppError::Font(e))
            }
        }

        Result::Ok(Application { screen, window, audio, font })
    }
}

impl Drop for Application {
    fn drop(&var self) {
        close_font(self.font)
        self.audio.shutdown()
        close_window(self.window)
        close_screen(self.screen)
    }
}
\end{lstlisting}

\begin{comingfromc}
In C, multi-step initialization often uses the ``goto cleanup'' pattern:

\begin{lstlisting}[language=C]
int init_app(App *app) {
    app->screen = OpenScreen(&spec);
    if (!app->screen) goto fail_screen;

    app->window = OpenWindow(&spec);
    if (!app->window) goto fail_window;

    app->audio = init_audio();
    if (!app->audio) goto fail_audio;

    return 0;  // Success

fail_audio:
    CloseWindow(app->window);
fail_window:
    CloseScreen(app->screen);
fail_screen:
    return -1;
}
\end{lstlisting}

While functional, this pattern is error-prone. Novus's RAII with \texttt{Drop} ensures cleanup happens automatically and in the correct order.
\end{comingfromc}

% ============================================================================
\section{Error Handling Best Practices}
\label{sec:error-best-practices}
% ============================================================================

\subsection{Do's}

\begin{itemize}
    \item \textbf{Do} use \texttt{?} for concise error propagation
    \item \textbf{Do} pattern match to handle specific error variants
    \item \textbf{Do} use \texttt{defer} for cleanup that must happen
    \item \textbf{Do} create custom error types for your domain
    \item \textbf{Do} implement \texttt{message()} for human-readable errors
    \item \textbf{Do} use \texttt{unwrap\_or} when a default makes sense
    \item \textbf{Do} test error paths, not just the happy path
    \item \textbf{Do} log errors before propagating in complex systems
\end{itemize}

\subsection{Don'ts}

\begin{itemize}
    \item \textbf{Don't} use \texttt{unwrap()} unless failure is impossible
    \item \textbf{Don't} ignore \texttt{Result} return values
    \item \textbf{Don't} use \texttt{panic!()} for recoverable errors
    \item \textbf{Don't} catch and re-throw without adding context
    \item \textbf{Don't} use sentinel values (0, -1, null) instead of \texttt{Result}
    \item \textbf{Don't} forget to handle all error variants in match
    \item \textbf{Don't} mix error handling strategies in the same codebase
\end{itemize}

\subsection{Error Design Guidelines}

When designing error types for a library or module:

\begin{enumerate}
    \item \textbf{Be specific}: Create variants for each distinct failure mode
    \item \textbf{Include context}: Add data that helps diagnose the problem
    \item \textbf{Be recoverable}: If an error can be retried or worked around, make that obvious
    \item \textbf{Be consistent}: Use similar patterns across your codebase
    \item \textbf{Document errors}: Each variant should have a doc comment
\end{enumerate}

\begin{lstlisting}
/// Errors that can occur during network operations
pub enum NetworkError {
    /// Connection to server failed
    /// Contains: host address, port
    ConnectionFailed { host: *u8, port: u16 },

    /// Connection timed out after max_wait milliseconds
    /// Recoverable: retry with longer timeout
    Timeout { operation: *u8, max_wait: u32 },

    /// Server sent invalid response
    /// Not recoverable: protocol error
    InvalidResponse { code: u32 },

    /// Network stack not initialized
    /// Recoverable: call network_init() first
    NotInitialized,
}
\end{lstlisting}
