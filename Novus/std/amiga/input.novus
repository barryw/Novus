// Window input is exposed as the toolkit-independent `amiga::ui::Event`.
// Device-level input APIs belong under `amiga::sys` until Tier 1 has a real
// application use case; duplicating the UI event model here would add a maze.
