// Generated from SFD file by Novus SFD Parser
// Library: label.library
// Base: _LabelBase

from amiga::raw::structs import *
//
// NOTE: Constants are in amiga::raw::consts
// NOTE: Structs are in amiga::raw::structs

// ============================================================================
// Library Functions
// ============================================================================

/// Gets the pointer to the label image class.
///
/// Obtains the pointer to the Label gadget class for use with
/// NewObject().  This function always returns a valid pointer so
/// you do not need to check it.  The reason is that if the library
/// opens fine, then the pointer returned is already setup.  (Of course
/// this implies that if opening the library fails, you shouldn't be
/// calling this.)
/// Note that this function does not create the class, that is done
/// when the class library is opened.
///
/// # Synopsis
///
/// label_class = LABEL_GetClass();
/// D0
///
/// Class * LABEL_GetClass(VOID);
///
/// # Arguments
///
/// * Nothing.
///
/// # Returns
///
/// label_class - Pointer to the Label gadget class.
@library("label.library")
extern pub fn LABEL_GetClass() -> *IClass
