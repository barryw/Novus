// Generated from SFD file by Novus SFD Parser
// Library: keymap.library
// Base: _KeymapBase
//
// NOTE: Constants are in std::ffi::amiga_consts
// NOTE: Structs are in std::ffi::amiga_structs

// ============================================================================
// Library Functions
// ============================================================================

/// Set the current default keymap. (V36)
///
/// A pointer to key map specified is cached by the keymap library for use by
/// MapRawKey and MapANSI when a keymap is not specified.
///
/// # Arguments
/// * `keyMap` - a pointer to a keyMap structure.  This key map must be pe...
///
/// See also: devices/keymap.h, keymap.library/AskKeyMapDefault(), console.device ...KEYMAP functions
extern pub fn SetKeyMapDefault(keyMap: *KeyMap)
/// Ask for a pointer to the current default
///
/// Return a pointer to the keymap used by the keymap library for MapRawKey and
/// MapANSI when a keymap is not specified.
///
/// # Returns
/// keyMap - a pointer to a keyMap structure.  This key map is
///
/// See also: devices/keymap.h, keymap.library/SetKeyMapDefault(), console.device ...KEYMAP functions
extern pub fn AskKeyMapDefault() -> *KeyMap
/// Decode single raw key input event to an ANSI
///
/// This console function converts input events of type IECLASS_RAWKEY to ANSI
/// bytes, based on the keyMap, and places the result into the buffer.
///
/// # Arguments
/// * `event` - an InputEvent structure pointer.  The event list is not t...
///
/// # Returns
/// actual - the number of characters in the buffer, or -1 if
///
/// See also: devices/inputevent.h, devices/keymap.h
extern pub fn MapRawKey(event: *InputEvent, buffer: *u8, length: i16, keyMap: *KeyMap) -> i16
/// Encode an ANSI string into keycodes. (V36)
///
/// This console function converts an ANSI byte string to the code/qualifier
/// pairs of type IECLASS_RAWKEY that would generate the string and places those
/// pairs in a buffer. A code/qualifier pair is a pair of bytes suitable for
/// putting in the ie_Code and low byte of ie_Qualifier, and for subsequent
/// events, shifted to the ie_Prev1DownCode/ ie_Prev1DownQual then
/// ie_Prev2DownCode/ie_Prev2DownQual pairs for any dead or double dead key
/// mapping.
///
/// # Arguments
/// * `string` - the ANSI string to convert. count - the number of charact...
///
/// # Returns
/// actual - the number of code/qualifier pairs in the buffer,
///
/// See also: devices/inputevent.h, devices/keymap.h
extern pub fn MapANSI(string: *u8, count: i32, buffer: *u8, length: i32, keyMap: *KeyMap) -> i32
