[project]
name = "example.library"
version = "1.0.0"
type = "library"

[build]
output = "example.library"
target = "amiga"

# Library-specific settings
[library]
base_name = "ExampleBase"
id_string = "example.library 1.0 (01 Nov 2025)"
version = 1
revision = 0

[dependencies]
# Add any library dependencies here
# For example:
# exec = { version = "*", library = "exec.library" }
