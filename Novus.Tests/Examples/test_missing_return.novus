// Test that we detect missing returns

fn might_return(x: i32) -> i32 {
    if x > 0 {
        return 42
    }
    // Missing return here - should error!
}

pub fn main() -> i32 {
    return might_return(5)
}
