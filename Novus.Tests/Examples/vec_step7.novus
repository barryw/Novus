from std::collections import Vec

// Step 7: Test the set() operation
fn main() -> i32 {
    var v: Vec<i32> = Vec::new()
    v.push(10)
    v.push(20)
    v.push(30)

    let ok: bool = v.set(1u32, 25)
    if !ok {
        return 1
    }

    v.drop()
    return 42
}
