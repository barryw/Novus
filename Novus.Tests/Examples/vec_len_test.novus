from std::collections import Vec

fn main() -> i32 {
    // Create empty vector
    var v: Vec<i32> = Vec::new()

    // Test: length should be 0
    if v.len() == 0u32 {
        return 42  // Success!
    }

    return 1  // Failed
}
