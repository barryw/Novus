from std::collections import Vec

// Step 8: Test the len() operation
fn main() -> i32 {
    var v: Vec<i32> = Vec::new()
    v.push(10)
    v.push(20)
    v.push(30)

    if v.len() != 3u32 {
        return 1
    }

    v.drop()
    return 42
}
