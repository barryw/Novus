// Auto-generated test runner
// Generated by: novus test
// Date: 2025-12-14 19:13:26

from std::io::file import write
from std::test::test import __test_begin, __test_get_failures, __test_reset, expect, expect_true, expect_false
from std::test::test import expect_eq_i32, expect_eq_u32, expect_eq_i16, expect_eq_u16
from std::test::test import expect_eq_i8, expect_eq_u8, expect_eq_bool
from std::test::test import expect_null, expect_not_null, fail

from channel_comprehensive_test import test_unbounded_creation, test_unbounded_send_recv, test_unbounded_fifo_ordering, test_unbounded_empty_channel, test_unbounded_signal_mask, test_unbounded_drain, test_unbounded_sender_clone, test_unbounded_multiple_recv_on_empty, test_bounded_creation, test_bounded_send_recv, test_bounded_fifo_ordering, test_bounded_empty_channel, test_bounded_signal_mask, test_bounded_blocking_send, test_oneshot_creation, test_oneshot_send_recv, test_oneshot_single_send_only, test_oneshot_single_recv_only, test_oneshot_empty_channel, test_oneshot_is_sent, test_oneshot_is_complete, test_oneshot_signal_mask

pub fn main() -> i32 {
    write("\n=== Novus Test Runner ===\n\n")

    var passed: u32 = 0
    var failed: u32 = 0
    var skipped: u32 = 0
    var total: u32 = 0

    // Test: Create unbounded channel
    write("Create unbounded channel... ")
    __test_reset()
    __test_begin("test_unbounded_creation")
    test_unbounded_creation()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: send and receive a value
    write("Unbounded: send and receive a value... ")
    __test_reset()
    __test_begin("test_unbounded_send_recv")
    test_unbounded_send_recv()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: FIFO ordering of messages
    write("Unbounded: FIFO ordering of messages... ")
    __test_reset()
    __test_begin("test_unbounded_fifo_ordering")
    test_unbounded_fifo_ordering()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: try_recv on empty channel returns None
    write("Unbounded: try_recv on empty channel returns None... ")
    __test_reset()
    __test_begin("test_unbounded_empty_channel")
    test_unbounded_empty_channel()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: signal mask is valid
    write("Unbounded: signal mask is valid... ")
    __test_reset()
    __test_begin("test_unbounded_signal_mask")
    test_unbounded_signal_mask()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: drain processes all messages
    write("Unbounded: drain processes all messages... ")
    __test_reset()
    __test_begin("test_unbounded_drain")
    test_unbounded_drain()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: sender clone works
    write("Unbounded: sender clone works... ")
    __test_reset()
    __test_begin("test_unbounded_sender_clone")
    test_unbounded_sender_clone()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Unbounded: multiple recv on empty channel returns None
    write("Unbounded: multiple recv on empty channel returns None... ")
    __test_reset()
    __test_begin("test_unbounded_multiple_recv_on_empty")
    test_unbounded_multiple_recv_on_empty()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Create bounded channel
    write("Create bounded channel... ")
    __test_reset()
    __test_begin("test_bounded_creation")
    test_bounded_creation()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Bounded: send and receive a value
    write("Bounded: send and receive a value... ")
    __test_reset()
    __test_begin("test_bounded_send_recv")
    test_bounded_send_recv()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Bounded: FIFO ordering with try_send
    write("Bounded: FIFO ordering with try_send... ")
    __test_reset()
    __test_begin("test_bounded_fifo_ordering")
    test_bounded_fifo_ordering()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Bounded: try_recv on empty channel returns None
    write("Bounded: try_recv on empty channel returns None... ")
    __test_reset()
    __test_begin("test_bounded_empty_channel")
    test_bounded_empty_channel()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Bounded: signal mask is valid
    write("Bounded: signal mask is valid... ")
    __test_reset()
    __test_begin("test_bounded_signal_mask")
    test_bounded_signal_mask()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Bounded: blocking send with responder task
    write("Bounded: blocking send with responder task... ")
    __test_reset()
    __test_begin("test_bounded_blocking_send")
    test_bounded_blocking_send()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Create oneshot channel
    write("Create oneshot channel... ")
    __test_reset()
    __test_begin("test_oneshot_creation")
    test_oneshot_creation()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: send and receive a value
    write("Oneshot: send and receive a value... ")
    __test_reset()
    __test_begin("test_oneshot_send_recv")
    test_oneshot_send_recv()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: can only send once
    write("Oneshot: can only send once... ")
    __test_reset()
    __test_begin("test_oneshot_single_send_only")
    test_oneshot_single_send_only()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: can only receive once
    write("Oneshot: can only receive once... ")
    __test_reset()
    __test_begin("test_oneshot_single_recv_only")
    test_oneshot_single_recv_only()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: try_recv on empty channel returns None
    write("Oneshot: try_recv on empty channel returns None... ")
    __test_reset()
    __test_begin("test_oneshot_empty_channel")
    test_oneshot_empty_channel()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: is_sent tracks send state
    write("Oneshot: is_sent tracks send state... ")
    __test_reset()
    __test_begin("test_oneshot_is_sent")
    test_oneshot_is_sent()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: is_complete tracks receive state
    write("Oneshot: is_complete tracks receive state... ")
    __test_reset()
    __test_begin("test_oneshot_is_complete")
    test_oneshot_is_complete()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    // Test: Oneshot: signal mask is valid
    write("Oneshot: signal mask is valid... ")
    __test_reset()
    __test_begin("test_oneshot_signal_mask")
    test_oneshot_signal_mask()
    if __test_get_failures() == 0 {
        write("\x9b1mPASS\x9b0m\n")
        passed++
    } else {
        write("\x9b7mFAIL\x9b0m\n")
        failed++
    }
    total++

    write("\n=== Results ===\n")
    write("Passed:  %lu\n", passed)
    write("Failed:  %lu\n", failed)
    write("Skipped: %lu\n", skipped)
    write("Total:   %lu\n", total)

    if failed > 0 {
        write("\n\x9b7m*** TESTS FAILED ***\x9b0m\n")
        return 1
    }

    write("\n\x9b1m*** ALL TESTS PASSED ***\x9b0m\n")
    return 0
}
