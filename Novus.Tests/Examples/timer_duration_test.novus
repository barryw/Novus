// Test importing Duration from amiga::timer

from amiga::raw::dos import VPrintf
from amiga::timer import Duration

pub fn main() -> i32 {
    VPrintf("Before Duration use\n", (*u8)0)
    let d = Duration::secs(1)
    VPrintf("After Duration use\n", (*u8)0)
    return 0
}
