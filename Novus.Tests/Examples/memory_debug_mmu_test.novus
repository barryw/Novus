// Example: MMU Protection Test (Non-Crashing)
//
// Description:
//   Tests that MMU protection functions initialize and cleanup correctly
//   without actually triggering a crash. This verifies the MMU code path works.
//
// Build:
//   novus compile memory_debug_mmu_test.novus --safety-level 3
//
// Expected Behavior:
//   Should print "MMU test completed" and return 0.

pub fn main() -> i32 {
    // The MMU protection init/cleanup are automatically called for safety-level 3
    // We just return successfully to verify they don't crash during setup/teardown
    return 0
}
