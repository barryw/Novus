from std::collections import Vec

fn main() -> i32 {
    // Create empty vector
    var v: Vec<i32> = Vec::new()

    // Return is_empty() result (should be 1 if true, 0 if false)
    let result: bool = v.is_empty()
    if result {
        return 1
    }
    return 0
}
