from std::core import Vec

fn main() -> i32 {
    var v: Vec<i32> = Vec::new()

    // Return the raw value of is_empty() to see what we're getting
    // Should return 1 for true, 0 for false
    if v.is_empty() {
        return 1
    }
    return 0
}
