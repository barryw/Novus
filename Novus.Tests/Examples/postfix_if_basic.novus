// Test basic postfix if with return statement
// Expected: returns 42

pub fn main() -> i32 {
    let x = 5
    let y = 10

    // Should return 42 because x < y is true
    return 42 if x < y

    return 0
}
