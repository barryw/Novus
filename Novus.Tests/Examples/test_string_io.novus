from std::io import WriteOut

pub fn main() -> u32 {
    // WriteOut returns Result<i32, DosError>
    // DosError is re-exported from std::io, so we don't need to import it separately
    // Use the throwaway pattern _ to discard the result
    let _ = WriteOut("Hello from String type, pub use, and _ pattern!")
    return 0
}
