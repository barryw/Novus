from std::core import Vec

fn main() -> i32 {
    // Create empty vector using associated function
    var v: Vec<i32> = Vec::new()

    // Test: should be empty (WITHOUT negation - should return 42 if empty)
    if v.is_empty() {
        return 42  // Success
    }

    return 1  // Failed - not empty
}
